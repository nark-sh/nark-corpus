package: "https-proxy-agent"
semver: ">=0.0.0"
contract_version: "1.0.0"
maintainer: "nark-sh"
last_verified: "2026-06-12"
coverage_status: "out-of-scope"
skip_reason: "low-callsite"
researched_at: "2026-04-02"
coverage_rationale: |
  HTTP agent adapter — creates proxy-aware http.Agent instances passed to other HTTP libraries. Constructor does not make network calls. No behavioral contracts to enforce; errors are handled by the consuming HTTP library (axios, node-fetch, etc.). Config-only pattern.
