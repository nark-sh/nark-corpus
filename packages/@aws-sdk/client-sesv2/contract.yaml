package: "@aws-sdk/client-sesv2"
semver: ">=3.0.0 <4.0.0"
contract_version: "1.2.0"
maintainer: "corpus-team"
last_verified: "2026-06-24"
coverage_status: covered
# Depth pass 3 (deepen-stream-3): added UpdateContactCommand, CreateExportJobCommand
status: production
evidence_quality: confirmed
coverage_score: 0.85
# coverage_score = 11 contracted / 13 total relevant API functions (11 contracted + 2 intentionally omitted)
# Previously contracted (9): send (generic), SendEmailCommand, SendBulkEmailCommand,
#   CreateEmailIdentityCommand, SendCustomVerificationEmailCommand,
#   CreateEmailTemplateCommand, CreateImportJobCommand,
#   UpdateEmailTemplateCommand, PutSuppressedDestinationCommand, CreateContactCommand
# Newly contracted this pass (2): UpdateContactCommand, CreateExportJobCommand
# Intentionally omitted (2):
#   TestRenderEmailTemplateCommand (dev/preview utility, not production code path)
#   CreateConfigurationSetCommand (infrastructure management, not SaaS app runtime)
# Admin/read-only omitted (not counted): Get*, List*, Delete*, Put*Identity*, BatchGetMetricData

functions:
  - name: send
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Sends any SES v2 command via SESv2Client. Primary operations include SendEmailCommand
      (transactional email), SendBulkEmailCommand (bulk), CreateContactCommand,
      CreateEmailIdentityCommand (domain verification), PutEmailIdentityMailFromAttributesCommand.
      Returns a Promise that rejects on any service error or network failure.

    postconditions:
      - id: sesv2-send-no-try-catch
        maturity: stable
        condition: >
          Any SES v2 service error or network failure: sending quota exceeded
          (SendingQuotaExceededException), identity not verified (NotFoundException on unverified email),
          message rejected (MessageRejected), account in sandbox mode (AccountSuspendedException),
          permission denied, throttling (TooManyRequestsException), or network failure.
        throws: >
          SESv2ServiceException subclass with error.name set to the specific error code.
          For throttling: TooManyRequestsException. For invalid sender: NotFoundException
          or MessageRejected. For sandbox restrictions: AccountSuspendedException.
        required_handling: >
          Caller MUST wrap client.send() in try-catch. SES v2 emails fail in
          sandbox mode (unverified recipients), on quota exhaustion, or when the
          sending identity is not verified. Unhandled rejections cause silent email
          delivery failures.

          Minimum handling:
            try {
              await sesv2Client.send(new SendEmailCommand({ ... }));
            } catch (err) {
              if (err instanceof SESv2ServiceException) {
                if (err.name === 'SendingQuotaExceededException') {
                  // Queue for retry
                }
                console.error(`SES v2 error [${err.name}]: ${err.message}`);
              }
              throw err;
            }
        sources:
          - "https://docs.aws.amazon.com/AWSJavaScriptSDK/v3/latest/client/sesv2/"
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_SendEmail.html"
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

  - name: SendEmailCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Sends a transactional email via Amazon SES v2. Supports Simple (plain text/HTML),
      Raw (MIME), and Templated message types. Returns SendEmailResponse with MessageId
      on success. Throws on all service errors — the MessageId is NOT a delivery confirmation,
      only an acceptance receipt.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_SendEmail.html
      # Error types confirmed from @aws-sdk/client-sesv2 TypeScript declarations (dist-types/commands/SendEmailCommand.d.ts)
      - id: sesv2-send-email-no-try-catch
        maturity: stable
        condition: >
          Any service error or network failure: account suspended permanently
          (AccountSuspendedException), sending paused (SendingPausedException),
          sending domain not verified (MailFromDomainNotVerifiedException),
          message rejected for invalid content (MessageRejected),
          configuration set / template not found (NotFoundException),
          invalid parameters (BadRequestException), quota exceeded (LimitExceededException),
          throttling (TooManyRequestsException), or network failure.
        throws: >
          SESv2ServiceException subclass. error.name identifies the specific error code:
          AccountSuspendedException (account permanently blocked — not retryable),
          SendingPausedException (account sending disabled — check SES console),
          MailFromDomainNotVerifiedException (DKIM/DMARC setup incomplete),
          MessageRejected (virus scan failed — do not retry same content),
          NotFoundException (configuration set or template name does not exist),
          BadRequestException (malformed input — not retryable),
          LimitExceededException (hourly/daily send quota exceeded),
          TooManyRequestsException (API rate limit — retry with backoff).
        required_handling: >
          Caller MUST wrap client.send(new SendEmailCommand(...)) in try-catch.
          SendingPausedException and AccountSuspendedException require manual
          intervention via SES console — not retryable by code. LimitExceededException
          and TooManyRequestsException should queue for delayed retry.

          Minimum handling:
            try {
              const result = await sesv2Client.send(new SendEmailCommand({ ... }));
              // result.MessageId is an acceptance receipt, NOT delivery confirmation
            } catch (err) {
              if (err instanceof SESv2ServiceException) {
                // AccountSuspendedException / SendingPausedException: alert ops team
                // MailFromDomainNotVerifiedException: check domain DKIM/DMARC setup
                // LimitExceededException / TooManyRequestsException: queue for retry
                console.error(`SES v2 SendEmail [${err.name}]: ${err.message}`);
              }
              throw err;
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_SendEmail.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: high

  - name: SendBulkEmailCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Sends templated email to multiple recipients in one API call. Returns HTTP 200
      with BulkEmailEntryResults array even when individual recipients FAIL.
      Each entry has a Status field that MUST be checked — failures are NOT thrown
      as exceptions. This is the most dangerous SESv2 silent failure pattern.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_SendBulkEmail.html
      # BulkEmailEntryResult type confirmed from dist-types/models/models_0.d.ts (lines 720-800)
      # AWS docs explicitly state: "Check each response object and retry any messages with a failure status"
      - id: sesv2-bulk-email-result-not-checked
        maturity: stable
        condition: >
          SendBulkEmailCommand returns HTTP 200 with BulkEmailEntryResults array.
          Each entry has Status field that can be SUCCESS, ACCOUNT_SUSPENDED,
          ACCOUNT_THROTTLED, ACCOUNT_DAILY_QUOTA_EXCEEDED, MESSAGE_REJECTED,
          MAIL_FROM_DOMAIN_NOT_VERIFIED, CONFIGURATION_SET_DOES_NOT_EXIST,
          TEMPLATE_DOES_NOT_EXIST, ACCOUNT_SENDING_PAUSED,
          CONFIGURATION_SET_SENDING_PAUSED, INVALID_PARAMETER, TRANSIENT_FAILURE,
          or FAILED. Non-SUCCESS statuses are NOT thrown as exceptions.
        throws: >
          Nothing thrown for per-recipient failures. The HTTP 200 response contains
          BulkEmailEntryResults[n].Status and BulkEmailEntryResults[n].Error.
          Account-level errors (AccountSuspendedException, TooManyRequestsException,
          LimitExceededException) ARE thrown and terminate the entire request.
        required_handling: >
          Caller MUST iterate over response.BulkEmailEntryResults and check each
          entry's Status. Recipients with non-SUCCESS status must be tracked and
          retried. AWS documentation explicitly states: "Check each response object
          and retry any messages with a failure status."

          Minimum handling:
            const response = await sesv2Client.send(new SendBulkEmailCommand({ ... }));
            const failed = response.BulkEmailEntryResults?.filter(
              r => r.Status !== 'SUCCESS'
            ) ?? [];
            if (failed.length > 0) {
              // Log and queue for retry
              failed.forEach(r => console.error(`Bulk email failed: ${r.Status} - ${r.Error}`));
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_SendBulkEmail.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DATA_LOSS
          production_behavior: silent_failure
          visibility: silent
          user_experience: lost_data
          incident_cost_range: high

      - id: sesv2-bulk-email-no-try-catch
        maturity: stable
        condition: >
          Account-level errors for SendBulkEmailCommand ARE thrown as exceptions:
          AccountSuspendedException, SendingPausedException, TooManyRequestsException,
          LimitExceededException, MailFromDomainNotVerifiedException, BadRequestException.
          These terminate the entire bulk send operation (no entries processed).
        throws: >
          SESv2ServiceException subclass. Same error hierarchy as SendEmailCommand.
          AccountSuspendedException and SendingPausedException are not retryable.
        required_handling: >
          Caller MUST also wrap client.send(new SendBulkEmailCommand(...)) in try-catch
          for account-level failures, in addition to checking per-entry BulkEmailEntryResults.
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_SendBulkEmail.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: high

  - name: CreateEmailIdentityCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Registers an email address or domain as a sending identity in SES v2. Must be called
      before that identity can be used as a From address. Returns DKIM tokens for DNS setup.
      Often called during onboarding flows or multi-tenant domain registration.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateEmailIdentity.html
      # Error types confirmed from dist-types/commands/CreateEmailIdentityCommand.d.ts
      - id: sesv2-create-identity-no-try-catch
        maturity: stable
        condition: >
          CreateEmailIdentityCommand can fail with:
          AlreadyExistsException (identity already registered — common in idempotent flows),
          LimitExceededException (AWS account identity quota reached — requires AWS support),
          ConcurrentModificationException (server-side conflict during concurrent identity creation),
          NotFoundException (invalid domain or parent resource — rare),
          BadRequestException (invalid email address or domain format),
          TooManyRequestsException (rate limit exceeded).
        throws: >
          SESv2ServiceException subclass. AlreadyExistsException (HTTP 400) is the most
          common: attempting to re-register an identity that already exists. This must be
          handled in multi-tenant flows where multiple users may register the same domain.
          LimitExceededException requires AWS quota increase — cannot be resolved in code.
          ConcurrentModificationException is a server fault (HTTP 500) — retry with backoff.
        required_handling: >
          Caller MUST wrap in try-catch. AlreadyExistsException should typically be treated
          as success (idempotent) in registration flows. LimitExceededException requires
          alerting ops team — no programmatic solution.

          Minimum handling:
            try {
              const result = await sesv2Client.send(new CreateEmailIdentityCommand({ EmailIdentity: domain }));
            } catch (err) {
              if (err instanceof SESv2ServiceException) {
                if (err.name === 'AlreadyExistsException') {
                  // Identity already exists — treat as success in idempotent flows
                  return;
                }
                if (err.name === 'LimitExceededException') {
                  // AWS identity quota reached — alert ops team
                }
                throw err;
              }
              throw err;
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateEmailIdentity.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

  - name: SendCustomVerificationEmailCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Sends a custom verification email to a recipient using a pre-created verification
      template. Used in customer-facing email verification flows. Throws NotFoundException
      when the template name does not exist — a configuration error that causes silent
      onboarding failures if the template has been deleted or misnamed.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_SendCustomVerificationEmail.html
      # Error types confirmed from dist-types/commands/SendCustomVerificationEmailCommand.d.ts
      - id: sesv2-custom-verification-no-try-catch
        maturity: stable
        condition: >
          SendCustomVerificationEmailCommand can fail with:
          NotFoundException (template name does not exist — template deleted or misspelled),
          MailFromDomainNotVerifiedException (sending domain unverified),
          MessageRejected (message content rejected — virus scan),
          SendingPausedException (account sending disabled),
          LimitExceededException (rate limit),
          TooManyRequestsException (API throttle),
          BadRequestException (invalid email address format).
        throws: >
          SESv2ServiceException subclass. NotFoundException is the most insidious error:
          if the custom verification template is deleted from SES while the application
          still references it by name, all verification emails silently fail. The error
          name is 'NotFoundException' with HTTP 404.
        required_handling: >
          Caller MUST wrap in try-catch. NotFoundException for a missing template is a
          configuration error that should trigger an immediate alert — it means no
          verification emails can be sent until the template is recreated.

          Minimum handling:
            try {
              await sesv2Client.send(new SendCustomVerificationEmailCommand({
                EmailAddress: userEmail,
                TemplateName: 'MyVerificationTemplate',
              }));
            } catch (err) {
              if (err instanceof SESv2ServiceException) {
                if (err.name === 'NotFoundException') {
                  // Verification template deleted — alert ops, no verification emails can be sent
                }
                console.error(`Custom verification email failed [${err.name}]: ${err.message}`);
              }
              throw err;
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_SendCustomVerificationEmail.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: silent
          user_experience: service_unavailable
          incident_cost_range: medium

  - name: CreateEmailTemplateCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Creates a new email template for use with SendEmailCommand (templated) and
      SendBulkEmailCommand. Template names must be unique. Called during application
      setup, deployment, or when dynamically creating templates per tenant.
      UpdateEmailTemplateCommand follows the same pattern with NotFoundException
      instead of AlreadyExistsException.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateEmailTemplate.html
      # Error types confirmed from dist-types/commands/CreateEmailTemplateCommand.d.ts
      # UpdateEmailTemplateCommand has same pattern: NotFoundException replaces AlreadyExistsException
      - id: sesv2-create-template-no-try-catch
        maturity: stable
        condition: >
          CreateEmailTemplateCommand can fail with:
          AlreadyExistsException (template with this name already exists — not idempotent),
          LimitExceededException (template quota exceeded — AWS default is 20,000 templates),
          BadRequestException (invalid template syntax — Handlebars-style variables malformed),
          TooManyRequestsException (API rate limit: max 1 request/second for template operations).
          UpdateEmailTemplateCommand similarly throws NotFoundException when template does not exist.
        throws: >
          SESv2ServiceException subclass. AlreadyExistsException (HTTP 400) means a template
          with that exact name already exists. Unlike CreateEmailIdentityCommand, template
          creation is NOT idempotent — AlreadyExistsException means a conflict must be resolved.
          BadRequestException for Handlebars syntax errors in the template body.
        required_handling: >
          Caller MUST wrap in try-catch. AlreadyExistsException must be handled explicitly —
          either use a unique name, check existence first, or use UpdateEmailTemplateCommand
          for existing templates. TooManyRequestsException requires throttling template
          operations to at most 1/second.
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateEmailTemplate.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

  - name: CreateImportJobCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Creates an asynchronous import job to bulk-import contacts into a SES v2 contact list
      or suppression list from an S3 CSV/JSON source. Returns a JobId immediately — the actual
      import runs asynchronously. The API call succeeding does NOT mean import succeeded.
      Job status must be polled via GetImportJobCommand.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateImportJob.html
      # Error types confirmed from dist-types/commands/CreateImportJobCommand.d.ts
      - id: sesv2-import-job-result-not-polled
        maturity: stable
        condition: >
          CreateImportJobCommand returns a JobId immediately with HTTP 200. The import
          job then runs asynchronously. If the caller does not poll GetImportJobCommand
          for job completion/failure, it will never know whether contacts were actually
          imported. A failed import job silently drops all contacts.
        throws: >
          Nothing thrown for job execution failures. The API only throws for request-level
          errors: BadRequestException (invalid S3 path, malformed destination),
          LimitExceededException (concurrent import job limit exceeded),
          TooManyRequestsException (API rate limit).
        required_handling: >
          Caller MUST poll GetImportJobCommand until JobStatus is COMPLETE or FAILED.
          A FAILED status with FailedRecordsS3Url indicates partial failures.

          Minimum handling:
            const { JobId } = await sesv2Client.send(new CreateImportJobCommand({ ... }));
            // Poll for job completion
            let status = 'CREATED';
            while (status !== 'COMPLETED' && status !== 'FAILED') {
              await new Promise(r => setTimeout(r, 5000)); // 5s poll interval
              const job = await sesv2Client.send(new GetImportJobCommand({ JobId }));
              status = job.JobStatus;
            }
            if (status === 'FAILED') {
              // Check job.FailedRecordsS3Url for details
              throw new Error(`SES import job ${JobId} failed`);
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateImportJob.html"
        severity: warning
        evidence_quality: confirmed
        business_impact:
          incident_label: DATA_LOSS
          production_behavior: silent_failure
          visibility: silent
          user_experience: lost_data
          incident_cost_range: medium

      - id: sesv2-import-job-no-try-catch
        maturity: stable
        condition: >
          CreateImportJobCommand throws on request-level failures:
          LimitExceededException (concurrent import job limit),
          BadRequestException (invalid ImportDataSource S3 path or invalid ImportDestination),
          TooManyRequestsException (rate limit).
        throws: >
          SESv2ServiceException subclass. LimitExceededException means too many concurrent
          import jobs are running — wait for existing jobs to complete before starting new ones.
        required_handling: >
          Caller MUST wrap in try-catch in addition to polling job status.
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateImportJob.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

  - name: UpdateEmailTemplateCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Updates the content of an existing email template (subject, HTML body, or text body).
      Companion to CreateEmailTemplateCommand — used when a template already exists and
      needs updated content, subject line, or layout. Rate-limited to 1 request per second.
      Throws NotFoundException when the template name does not exist.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_UpdateEmailTemplate.html
      # Error types confirmed from AWS SES V2 API Reference documentation
      - id: sesv2-update-template-no-try-catch
        maturity: stable
        condition: >
          UpdateEmailTemplateCommand can fail with:
          NotFoundException (template with this name does not exist — must use CreateEmailTemplateCommand first),
          BadRequestException (invalid template syntax — malformed Handlebars variables or HTML),
          TooManyRequestsException (API rate limit: max 1 request/second for template operations).
        throws: >
          SESv2ServiceException subclass. NotFoundException (HTTP 404) is the primary concern:
          if the template was deleted externally (via console, another deploy, or cleanup script)
          while application code still calls UpdateEmailTemplateCommand, the update silently fails
          in a broad catch handler. BadRequestException for malformed Handlebars syntax in subject
          or body fields. TooManyRequestsException at > 1 req/sec — common in template sync jobs.
        required_handling: >
          Caller MUST wrap in try-catch. NotFoundException means the template no longer exists
          and must be recreated with CreateEmailTemplateCommand. BadRequestException indicates
          a template content bug — log the template name for debugging.

          Minimum handling:
            try {
              await sesv2Client.send(new UpdateEmailTemplateCommand({
                TemplateName: templateName,
                TemplateContent: { Subject: subject, Html: htmlBody },
              }));
            } catch (err) {
              if (err instanceof SESv2ServiceException) {
                if (err.name === 'NotFoundException') {
                  // Template deleted — recreate with CreateEmailTemplateCommand
                  await sesv2Client.send(new CreateEmailTemplateCommand({ ... }));
                }
                if (err.name === 'TooManyRequestsException') {
                  // Rate limit — add delay between template sync operations
                }
                console.error(`SES template update failed [${err.name}]: ${err.message}`);
              }
              throw err;
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_UpdateEmailTemplate.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

  - name: PutSuppressedDestinationCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Adds an email address to the account-level suppression list, preventing future email
      delivery to that address. MUST be called when processing bounce or complaint webhooks
      to maintain sender reputation. Required for CAN-SPAM and GDPR compliance workflows.
      Reason must be BOUNCE or COMPLAINT. The operation is effectively idempotent —
      calling it multiple times with the same address results in the same suppressed state.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_PutSuppressedDestination.html
      # Error types confirmed from AWS SES V2 API Reference documentation
      - id: sesv2-suppressed-destination-no-try-catch
        maturity: stable
        condition: >
          PutSuppressedDestinationCommand can fail with:
          BadRequestException (invalid EmailAddress format or invalid Reason value — must be BOUNCE or COMPLAINT),
          TooManyRequestsException (API rate limit exceeded — common when processing large bounce batches).
        throws: >
          SESv2ServiceException subclass. BadRequestException (HTTP 400) when Reason is not
          "BOUNCE" or "COMPLAINT", or when EmailAddress is malformed. TooManyRequestsException
          (HTTP 429) when suppression calls are batched too quickly after processing a large
          bounce or complaint batch from SES event webhooks.
        required_handling: >
          Caller MUST wrap in try-catch. This is typically called inside bounce/complaint
          webhook handlers — an unhandled exception here causes the webhook to return 5xx,
          which triggers SES retry storms. BadRequestException should log the address for
          manual review. TooManyRequestsException requires backoff between batch operations.

          Minimum handling:
            try {
              await sesv2Client.send(new PutSuppressedDestinationCommand({
                EmailAddress: bouncedAddress,
                Reason: 'BOUNCE', // or 'COMPLAINT'
              }));
            } catch (err) {
              if (err instanceof SESv2ServiceException) {
                if (err.name === 'TooManyRequestsException') {
                  // Rate limited — retry with exponential backoff
                }
                console.error(`Suppression failed [${err.name}]: ${err.message}`);
              }
              throw err;
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_PutSuppressedDestination.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: silent
          user_experience: service_unavailable
          incident_cost_range: medium

  - name: CreateContactCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Adds a new contact to a SES v2 contact list. Used in marketing/newsletter subscription
      flows to track opted-in email addresses with topic preferences. Throws AlreadyExistsException
      if the contact already exists in the list (not idempotent). Throws NotFoundException
      if the contact list itself does not exist. Setting UnsubscribeAll=true immediately opts
      the contact out of all topics despite any TopicPreferences provided.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateContact.html
      # Error types confirmed from AWS SES V2 API Reference documentation
      - id: sesv2-create-contact-no-try-catch
        maturity: stable
        condition: >
          CreateContactCommand can fail with:
          AlreadyExistsException (contact with this email already exists in the contact list),
          NotFoundException (contact list specified in ContactListName does not exist),
          BadRequestException (invalid email address format or malformed TopicPreferences),
          TooManyRequestsException (rate limit exceeded).
        throws: >
          SESv2ServiceException subclass. AlreadyExistsException (HTTP 400) is the most
          common: any subscription flow where users can resubscribe will hit this. Unlike
          CreateEmailIdentityCommand, treating AlreadyExistsException as success is the
          typical correct pattern for resubscription flows.
          NotFoundException (HTTP 404) means the contact list was deleted — the entire
          subscription flow is broken until the list is recreated.
          BadRequestException for invalid email format (e.g. addresses not meeting RFC 5321).
        required_handling: >
          Caller MUST wrap in try-catch. AlreadyExistsException should typically update the
          contact's subscription status using UpdateContactCommand rather than failing silently.
          NotFoundException requires immediate alerting — all new subscriptions are failing.

          Minimum handling:
            try {
              await sesv2Client.send(new CreateContactCommand({
                ContactListName: 'MyNewsletter',
                EmailAddress: userEmail,
                TopicPreferences: [{ TopicName: 'updates', SubscriptionStatus: 'OPT_IN' }],
              }));
            } catch (err) {
              if (err instanceof SESv2ServiceException) {
                if (err.name === 'AlreadyExistsException') {
                  // Contact exists — update subscription preferences instead
                  await sesv2Client.send(new UpdateContactCommand({ ... }));
                  return;
                }
                if (err.name === 'NotFoundException') {
                  // Contact list missing — alert ops team
                }
                console.error(`SES contact creation failed [${err.name}]: ${err.message}`);
              }
              throw err;
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateContact.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DATA_LOSS
          production_behavior: immediate_exception
          visibility: silent
          user_experience: lost_data
          incident_cost_range: medium

  - name: UpdateContactCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Updates a contact's topic preferences or unsubscribe status in a SES v2 contact list.
      Companion to CreateContactCommand — used in resubscription / preference-update flows
      where CreateContactCommand throws AlreadyExistsException. CRITICAL semantics: the
      TopicPreferences array REPLACES all existing topic preferences, not merges with them.
      Omitting a topic from the array silently removes the contact's preference for that
      topic. UnsubscribeAll=true is a hard override that opts the contact out regardless
      of TopicPreferences. Throws NotFoundException when either the contact list or the
      contact within it does not exist.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_UpdateContact.html
      # Error types confirmed from dist-types/commands/UpdateContactCommand.d.ts @throws annotations
      - id: sesv2-update-contact-no-try-catch
        maturity: stable
        condition: >
          UpdateContactCommand can fail with:
          NotFoundException (contact list does not exist OR contact with this email not found in list),
          BadRequestException (invalid TopicPreferences shape, invalid SubscriptionStatus value, or malformed email),
          ConcurrentModificationException (server-side conflict during concurrent contact updates — HTTP 500),
          TooManyRequestsException (API rate limit exceeded — common during bulk preference-sync jobs).
        throws: >
          SESv2ServiceException subclass. NotFoundException (HTTP 404) is the most common
          failure mode: it conflates "contact list missing" with "contact missing in list",
          so error handling must read error.message to distinguish them. In webhook handlers
          processing preference-change events, a NotFoundException silently drops the
          preference update unless caught. ConcurrentModificationException is a server fault
          retryable with exponential backoff.
        required_handling: >
          Caller MUST wrap client.send(new UpdateContactCommand(...)) in try-catch.
          NotFoundException requires distinguishing list-missing (ops alert) from
          contact-missing (typically: fall back to CreateContactCommand to upsert the
          contact). ConcurrentModificationException should retry with backoff.

          Minimum handling:
            try {
              await sesv2Client.send(new UpdateContactCommand({
                ContactListName: 'MyNewsletter',
                EmailAddress: userEmail,
                TopicPreferences: allDesiredTopics, // MUST include all topics, not just changed ones
              }));
            } catch (err) {
              if (err instanceof SESv2ServiceException) {
                if (err.name === 'NotFoundException') {
                  // Distinguish list-missing vs contact-missing via err.message
                  // For contact-missing: fall back to CreateContactCommand (upsert)
                  await sesv2Client.send(new CreateContactCommand({ ... }));
                  return;
                }
                if (err.name === 'ConcurrentModificationException') {
                  // Server-side conflict — retry with exponential backoff
                }
                console.error(`SES contact update failed [${err.name}]: ${err.message}`);
              }
              throw err;
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_UpdateContact.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DATA_LOSS
          production_behavior: immediate_exception
          visibility: silent
          user_experience: lost_data
          incident_cost_range: medium

  - name: CreateExportJobCommand
    import_path: "@aws-sdk/client-sesv2"
    description: >
      Creates an asynchronous export job that exports SES metrics data or message insights
      to an S3 destination as CSV or JSON. Returns a JobId immediately with HTTP 200 — the
      actual export runs asynchronously in the background. The API call succeeding does NOT
      mean the export succeeded. Job status must be polled via GetExportJobCommand. Same
      silent-failure async-poll pattern as CreateImportJobCommand. Rate-limited to 1 request
      per second.

    postconditions:
      # Evidence: https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateExportJob.html
      # Error types confirmed from dist-types/commands/CreateExportJobCommand.d.ts @throws annotations
      - id: sesv2-export-job-result-not-polled
        maturity: stable
        condition: >
          CreateExportJobCommand returns a JobId immediately with HTTP 200. The export
          job then runs asynchronously. If the caller does not poll GetExportJobCommand
          for COMPLETED / FAILED / CANCELLED status, it will never know whether the export
          actually produced output. A FAILED export job silently drops all requested data,
          and reporting / analytics pipelines downstream see an empty S3 bucket they
          assume contains complete data.
        throws: >
          Nothing thrown for job execution failures. The API only throws for request-level
          errors: BadRequestException (invalid ExportDataSource or ExportDestination S3 URL),
          LimitExceededException (concurrent export job limit exceeded — AWS account quota),
          NotFoundException (referenced configuration set or message-insights filter
          target does not exist), TooManyRequestsException (API rate limit > 1 req/sec).
        required_handling: >
          Caller MUST poll GetExportJobCommand until JobStatus is COMPLETED, FAILED, or
          CANCELLED. A FAILED status with FailureInfo indicates the export did not
          produce the expected S3 output — downstream analytics MUST be paused until
          the failure is investigated.

          Minimum handling:
            const { JobId } = await sesv2Client.send(new CreateExportJobCommand({ ... }));
            // Poll for job completion
            let status = 'CREATED';
            while (status !== 'COMPLETED' && status !== 'FAILED' && status !== 'CANCELLED') {
              await new Promise(r => setTimeout(r, 5000)); // 5s poll interval
              const job = await sesv2Client.send(new GetExportJobCommand({ JobId }));
              status = job.JobStatus;
            }
            if (status !== 'COMPLETED') {
              // Check job.FailureInfo for details; downstream analytics must NOT consume S3 output
              throw new Error(`SES export job ${JobId} ended with status ${status}`);
            }
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateExportJob.html"
        severity: warning
        evidence_quality: confirmed
        business_impact:
          incident_label: DATA_LOSS
          production_behavior: silent_failure
          visibility: silent
          user_experience: lost_data
          incident_cost_range: medium

      - id: sesv2-export-job-no-try-catch
        maturity: stable
        condition: >
          CreateExportJobCommand throws on request-level failures:
          LimitExceededException (concurrent export job limit reached — must wait for
          existing jobs to finish), BadRequestException (invalid ExportDataSource shape
          or invalid S3Url destination), NotFoundException (referenced resource missing),
          TooManyRequestsException (API rate limit > 1 req/sec, common in scheduled
          export jobs that fire on a cron).
        throws: >
          SESv2ServiceException subclass. LimitExceededException means too many concurrent
          export jobs are running — caller must wait for existing jobs to complete before
          starting new ones. TooManyRequestsException requires 1-second minimum spacing
          between calls.
        required_handling: >
          Caller MUST wrap client.send(new CreateExportJobCommand(...)) in try-catch in
          addition to polling job status. LimitExceededException should defer the export
          and retry later (not immediately). BadRequestException indicates a programming
          error — log the request shape for debugging.
        sources:
          - "https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_CreateExportJob.html"
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

tags:
  - aws
  - email
  - ses
  - sesv2
  - transactional-email
  - error-handling
  - network
  - sdk

notes:
  - "Scan 2026-04-11: scan encountered errors (0 repos scanned due to scanner failure on only available repo). FP check: 0/20 repos. evidence_quality upgraded to confirmed based on FP check passing and existing evidence."
  - "Depth pass 2026-04-16: 5 new command-specific function entries added (SendEmailCommand, SendBulkEmailCommand, CreateEmailIdentityCommand, SendCustomVerificationEmailCommand, CreateEmailTemplateCommand, CreateImportJobCommand)."
  - "SESv2 uses different parameters than SES v1: FromEmailAddress (not Source), Content.Simple (not Message)"
  - "SESv2Client is a separate class from SESClient — different import paths"
  - "Error class is SESv2ServiceException, not SESServiceException"
  - "Sandbox mode: only verified email addresses can receive emails by default"
  - "TooManyRequestsException: implement exponential backoff for high-volume sending"
  - "SendBulkEmailCommand: CRITICAL silent failure — HTTP 200 with per-recipient status in BulkEmailEntryResults"
  - "CreateImportJobCommand: async job pattern — always poll GetImportJobCommand for COMPLETED/FAILED status"
  - "Depth pass 2 (2026-04-16): 3 new command-specific function entries added (UpdateEmailTemplateCommand, PutSuppressedDestinationCommand, CreateContactCommand). Coverage 75%→82%."
  - "UpdateEmailTemplateCommand: rate-limited to 1 req/sec — common in template sync deployment scripts"
  - "PutSuppressedDestinationCommand: MUST be called in bounce/complaint webhook handlers — failure causes webhook 5xx retry storms"
  - "CreateContactCommand: AlreadyExistsException is expected in resubscription flows — treat as UpdateContactCommand trigger, not fatal error"
  - "CreateContactCommand: NotFoundException means contact list missing — all new subscriptions failing, requires immediate ops alert"
  - "Depth pass 3 (2026-06-24): 2 new command-specific function entries added (UpdateContactCommand, CreateExportJobCommand). Coverage 82%→85%."
  - "UpdateContactCommand: TopicPreferences array REPLACES (not merges) existing preferences — omitting a topic silently removes the contact's opt-in for that topic"
  - "UpdateContactCommand: NotFoundException conflates list-missing and contact-missing — must read err.message to distinguish; contact-missing typically routes to CreateContactCommand fallback"
  - "CreateExportJobCommand: async job pattern like CreateImportJobCommand — always poll GetExportJobCommand for COMPLETED/FAILED/CANCELLED status"
  - "CreateExportJobCommand: FAILED status with FailureInfo must pause downstream analytics that consume the S3 export bucket"

related_packages:
  - "@aws-sdk/client-ses"
  - "@aws-sdk/client-sns"
  - "nodemailer"
  - "resend"
