package: "@radix-ui/react-avatar"
semver: ">=0.0.0"
contract_version: "1.0.0"
maintainer: "nark-sh"
last_verified: "2026-06-10"
coverage_status: "no-async-contract"
researched_at: "2026-06-10"
coverage_rationale: |
  Headless React component primitives for avatars with image-load fallback. Image-load failures are handled internally via the AvatarFallback slot, not surfaced as exceptions to caller code. No async error contract worth enforcing.
