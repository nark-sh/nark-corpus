package: "class-validator"
semver: ">=0.0.0"
contract_version: "1.0.0"
maintainer: "nark-sh"
last_verified: "2026-06-12"
coverage_status: "out-of-scope"
skip_reason: "low-callsite"
researched_at: "2026-04-02"
coverage_rationale: |
  Decorator-based validation library. Usage is config/annotation style only (IsEmail, IsString decorators). No explicit validate() call sites in application code across corpus repos. NestJS consumes it via ValidationPipe internally.
