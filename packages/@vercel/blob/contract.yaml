package: '@vercel/blob'
semver: '>=0.19.0'
contract_version: 1.3.0
maintainer: corpus-team
last_verified: '2026-06-24'
coverage_status: covered
status: production
evidence_quality: confirmed
# Depth pass 2026-06-24 (deepen-stream-2 pass 42, drift-by-staleness mode):
# Installed version probed: 2.4.1
# API surface: 20 distinct async-callable functions (server + client exports, incl. v2 presigned-URL flow)
# Contracted before: 14 (put, del, list, head, get, copy, handleUpload, createMultipartUpload,
#                         uploadPart, completeMultipartUpload, upload,
#                         generateClientTokenFromReadWriteToken, createMultipartUploader, createFolder)
# Contracted after: 18 (+4 this pass: issueSignedToken, presignUrl, handleUploadPresigned,
#                         uploadPresigned — all introduced in @vercel/blob v2.x presigned-URL flow)
# Omitted: 2 (parseStoreIdFromDelegationToken sync, parseStoreIdFromPresignedUrl sync — both
#              synchronous helpers with no I/O), plus the 6 client multipart functions previously
#              omitted (same error profiles as server versions, getDownloadUrl sync,
#              getPayloadFromClientToken sync, client.put with same profile as upload)
# Coverage: 18/20 = 0.90 (before: 14/16 = 0.88)
# Evidence: @vercel/blob v2.4.1 source — dist/index.d.ts + dist/client.d.ts + dist/chunk-HZSIXSMY.cjs
#   https://vercel.com/docs/vercel-blob/using-blob-sdk
#
# Depth pass 2026-04-16 (deepen-stream-2 pass 10):
# +4 functions: upload, generateClientTokenFromReadWriteToken, createMultipartUploader, createFolder
coverage_score: 0.9
functions:
  - name: put
    import_path: '@vercel/blob'
    description: >
      Uploads a blob (file, string, Buffer, ReadableStream) to Vercel Blob storage. Requires BLOB_READ_WRITE_TOKEN env
      var. Returns BlobObject with url, pathname, contentType, contentDisposition. Throws on auth failure, quota
      exceeded, or network error.
    postconditions:
      # Evidence: seen in 18 primary firings across 6 repos in scan-vercel-blob-2026-04-13.json
      # Top affected: mfts/papermark, ethanniser/NextFaster, OpenHealthForAll/open-health
      - id: blob-put-no-try-catch
        condition: >
          Missing BLOB_READ_WRITE_TOKEN (BlobAccessError), content-type not allowed (BlobContentTypeNotAllowedError),
          storage quota exceeded (BlobStoreLimitReachedError), network failure, or Vercel API unavailable
          (BlobUnknownError).
        throws: >
          BlobAccessError, BlobContentTypeNotAllowedError, BlobStoreLimitReachedError, or BlobUnknownError. All extend
          BlobError which extends Error.
        required_handling: >
          Caller MUST wrap put() in try-catch. Missing BLOB_READ_WRITE_TOKEN causes immediate BlobAccessError (common in
          deployments that forgot to set the env var). Storage quota errors are hard to predict and will crash file
          upload endpoints if unhandled.

          Minimum handling:
            try {
              const blob = await put('file.pdf', content, { access: 'public' });
              return blob.url;
            } catch (err) {
              if (err instanceof BlobAccessError) {
                throw new Error('Blob storage not configured');
              }
              throw err;
            }
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
          - https://vercel.com/docs/storage/vercel-blob/api-reference
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
  - name: del
    import_path: '@vercel/blob'
    description: >
      Deletes one or more blobs by URL. Can delete a single URL or array of URLs. Throws on auth failure or network
      error.
    postconditions:
      - id: blob-del-no-try-catch
        condition: |
          Missing auth token, network failure, or Vercel API unavailable.
        throws: |
          BlobAccessError or BlobUnknownError.
        required_handling: |
          Caller MUST wrap del() in try-catch. Failed deletions leave orphaned blobs that incur storage costs.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/api-reference
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
  - name: list
    import_path: '@vercel/blob'
    description: >
      Lists blobs in the store, with optional prefix filtering and pagination. Returns { blobs, cursor, hasMore }.
      Throws on auth failure or network error.
    postconditions:
      - id: blob-list-no-try-catch
        condition: |
          Missing auth token or network failure.
        throws: |
          BlobAccessError or BlobUnknownError.
        required_handling: |
          Caller MUST wrap list() in try-catch.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/api-reference
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
  - name: head
    import_path: '@vercel/blob'
    description: >
      Fetches metadata of a blob object (size, uploadedAt, pathname, contentType, contentDisposition, url, downloadUrl,
      cacheControl, etag). Unlike get(), head() does NOT return null on missing blobs — it throws BlobNotFoundError.
      This is a common footgun when developers assume head() behaves like a safe existence check.
    postconditions:
      - id: blob-head-no-try-catch
        severity: error
        description: >
          head() THROWS BlobNotFoundError when the blob does not exist — it does NOT return null. This is distinct from
          get() which returns null on 404. Code that calls head() without try-catch will crash with an unhandled
          exception if the blob was deleted or the pathname is wrong.
        throws: BlobNotFoundError, BlobAccessError, BlobStoreNotFoundError, or BlobUnknownError.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: head() THROWS BlobNotFoundError when the blob does not exist — it does NOT return null.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
      - id: blob-head-access-error
        severity: error
        description: >
          head() throws BlobAccessError when BLOB_READ_WRITE_TOKEN is missing, invalid, or the token does not have
          access to the requested store. BlobStoreNotFoundError is thrown when the store ID in the token references a
          deleted or nonexistent store.
        throws: BlobAccessError or BlobStoreNotFoundError.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: >-
          head() throws BlobAccessError when BLOB_READ_WRITE_TOKEN is missing, invalid, or the token does not have
          access to the requested store.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
  - name: copy
    import_path: '@vercel/blob'
    description: >
      Copies a blob to a new pathname in the same store. Does NOT copy contentType or cacheControlMaxAge from the source
      — these must be explicitly specified if preservation is required. addRandomSuffix defaults to false (unlike
      put()). Throws on auth failure, source not found, precondition mismatch, or network error.
    postconditions:
      - id: blob-copy-no-try-catch
        severity: error
        description: >
          copy() throws on any error: BlobAccessError (auth failure), BlobNotFoundError (source blob doesn't exist),
          BlobUnknownError (network failure), or BlobPreconditionFailedError (ETag mismatch when ifMatch is used). No
          try-catch causes immediate exception.
        throws: BlobAccessError, BlobNotFoundError, BlobUnknownError, or BlobPreconditionFailedError.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: >-
          copy() throws on any error: BlobAccessError (auth failure), BlobNotFoundError (source blob doesn't exist),
          BlobUnknownError (network failure), or BlobPreconditionFailedError (ETag mismatch when ifMatch is used).
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
      - id: blob-copy-source-not-found
        severity: error
        description: >
          copy() throws BlobNotFoundError when the source blob (fromUrlOrPathname) does not exist. This is a common race
          condition: the source was deleted between when it was listed/referenced and when the copy is attempted.
        throws: BlobNotFoundError.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: copy() throws BlobNotFoundError when the source blob (fromUrlOrPathname) does not exist.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
  - name: get
    import_path: '@vercel/blob'
    description: >
      Fetches the content of a blob as a ReadableStream. Returns GetBlobResult with { statusCode, stream, headers, blob
      } or NULL if the blob is not found (HTTP 404). Unlike head(), get() returns null on not-found instead of throwing.
      Callers MUST check for null before using the stream. Also throws on auth failure or service unavailability.
    postconditions:
      - id: blob-get-null-not-checked
        severity: error
        description: >
          get() returns null when the blob is not found (HTTP 404) — it does NOT throw. Code that accesses result.stream
          or result.blob without null-checking will throw "Cannot read properties of null" at runtime. This is a silent
          crash disguised as a TypeError instead of a domain error.
        returns: GetBlobResult | null (null when blob not found — callers must null-check before accessing stream or blob).
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: get() returns null when the blob is not found (HTTP 404) — it does NOT throw.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
      - id: blob-get-no-try-catch
        severity: error
        description: >
          get() throws BlobAccessError (auth failure), BlobServiceRateLimited (HTTP 429, retryAfter seconds available on
          error), or BlobServiceNotAvailable (Vercel API down). These must be caught separately from the null check for
          not-found. A null check alone does not protect against thrown errors.
        throws: BlobAccessError, BlobServiceRateLimited, or BlobServiceNotAvailable.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: >-
          get() throws BlobAccessError (auth failure), BlobServiceRateLimited (HTTP 429, retryAfter seconds available on
          error), or BlobServiceNotAvailable (Vercel API down).
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
  - name: handleUpload
    import_path: '@vercel/blob/client'
    description: >
      Server-side route handler for client uploads (browser-to-blob). Has two responsibilities: (1) generate client
      tokens before upload (onBeforeGenerateToken), (2) receive completion callbacks (onUploadCompleted). The
      onBeforeGenerateToken callback MUST authenticate the user — if skipped, anonymous users can upload to your store.
      handleUpload() itself throws BlobAccessError if the server-side BLOB_READ_WRITE_TOKEN is missing.
    postconditions:
      - id: blob-handle-upload-missing-auth-check
        severity: error
        description: >
          onBeforeGenerateToken must authenticate the user before returning token options. If onBeforeGenerateToken
          returns without verifying session/auth, any unauthenticated client can generate upload tokens and write to
          your Blob store. This is an open upload endpoint vulnerability.
        throws: BlobAccessError or any error thrown from onBeforeGenerateToken/onUploadCompleted callbacks.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: onBeforeGenerateToken must authenticate the user before returning token options.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
      - id: blob-handle-upload-no-try-catch
        severity: error
        description: >
          handleUpload() itself throws BlobAccessError when BLOB_READ_WRITE_TOKEN is missing or invalid. It also
          re-throws any error thrown from onBeforeGenerateToken or onUploadCompleted callbacks. The outer route handler
          must wrap handleUpload() in try-catch and return an HTTP error response, not let the exception propagate.
        throws: BlobAccessError when BLOB_READ_WRITE_TOKEN is missing or invalid, or re-throws from callbacks.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: handleUpload() itself throws BlobAccessError when BLOB_READ_WRITE_TOKEN is missing or invalid.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
  - name: createMultipartUpload
    import_path: '@vercel/blob'
    description: >
      Creates a multipart upload session. Returns { key, uploadId } needed for subsequent uploadPart() and
      completeMultipartUpload() calls. Part of the manual three-phase multipart upload API. Throws same error types as
      put().
    postconditions:
      - id: blob-create-multipart-no-try-catch
        severity: error
        description: >
          createMultipartUpload() throws BlobAccessError (auth failure), BlobServiceRateLimited (rate limit with
          retryAfter), or BlobUnknownError (network failure). An uncaught failure in Phase 1 of the multipart upload
          leaves no orphaned state (upload session was never created) but the caller must handle the error to surface
          the failure to the user.
        throws: BlobAccessError, BlobServiceRateLimited, or BlobUnknownError.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: >-
          createMultipartUpload() throws BlobAccessError (auth failure), BlobServiceRateLimited (rate limit with
          retryAfter), or BlobUnknownError (network failure).
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
  - name: uploadPart
    import_path: '@vercel/blob'
    description: >
      Uploads a single part of a multipart upload. Each part must be at least 5MB except the last part. Parts can be
      uploaded in parallel. Returns { etag, partNumber } needed for completeMultipartUpload(). Throws on auth failure,
      network error, or part size violation.
    postconditions:
      - id: blob-upload-part-no-try-catch
        severity: error
        description: >
          uploadPart() throws BlobAccessError, BlobFileTooLargeError, BlobUnknownError, or BlobServiceRateLimited. When
          uploading parts in parallel (the recommended pattern), a single failed part rejection in a Promise.all() will
          cancel the entire batch. The uploadId and key are now in an incomplete state — the caller must either retry
          the failed part or abandon the upload entirely.
        throws: BlobAccessError, BlobFileTooLargeError, BlobUnknownError, or BlobServiceRateLimited.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: uploadPart() throws BlobAccessError, BlobFileTooLargeError, BlobUnknownError, or BlobServiceRateLimited.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
      - id: blob-upload-part-minimum-size
        severity: warning
        description: >
          Each part (except the last) must be at least 5MB. Passing parts smaller than 5MB will cause
          BlobFileTooLargeError (despite the misleading name — it's actually a "too small" violation). Common mistake:
          splitting files by count instead of by byte size, or passing the last chunk as part N-1 when another chunk
          follows.
        throws: BlobFileTooLargeError when a part is smaller than 5MB (except the last part).
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: Each part (except the last) must be at least 5MB.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
  - name: completeMultipartUpload
    import_path: '@vercel/blob'
    description: >
      Finalizes a multipart upload by assembling all parts. Requires the uploadId and key from createMultipartUpload(),
      and the array of Part objects from uploadPart() calls. Returns PutBlobResult with url, pathname, etc. Parts must
      be provided in order (by partNumber). Throws on auth failure, invalid parts array, or network error.
    postconditions:
      - id: blob-complete-multipart-no-try-catch
        severity: error
        description: >
          completeMultipartUpload() throws BlobAccessError (auth failure), BlobUnknownError (network failure or invalid
          parts), or BlobServiceNotAvailable. If called with missing or corrupted parts (e.g., some parts failed and
          weren't retried), the API returns a bad_request error mapped to BlobError. The blob is NOT created if
          completeMultipartUpload() fails — no partial blob is persisted.
        throws: BlobAccessError, BlobUnknownError, or BlobServiceNotAvailable.
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed
        required_handling: Caller MUST handle this condition appropriately.
        condition: >-
          completeMultipartUpload() throws BlobAccessError (auth failure), BlobUnknownError (network failure or invalid
          parts), or BlobServiceNotAvailable.
        sources:
          - https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
  - name: upload
    import_path: '@vercel/blob/client'
    description: >
      Client-side upload function for browser environments. Unlike server-side put(), upload() fetches
      a client token from the server's handleUpload route before uploading. This is the primary browser
      upload API for Vercel Blob. Requires handleUploadUrl pointing to a server route that implements
      handleUpload(). Throws BlobError if handleUploadUrl is missing, if the token fetch fails, or if
      the blob storage API rejects the upload.

    postconditions:
      # Evidence: dist/client.cjs lines 56-90 — upload() wraps createPutMethod with getToken()
      # that calls retrieveClientToken() which fetches from handleUploadUrl.
      # Error confirmed: BlobError("Failed to retrieve the client token") if server returns non-2xx.
      # Source: https://vercel.com/docs/vercel-blob/using-blob-sdk#client-uploads
      - id: blob-upload-missing-handle-url
        condition: >
          handleUploadUrl option is not provided to upload(). This is a required parameter —
          upload() cannot work without a server route to fetch a client token from.
        throws: "BlobError: client/`upload` requires the 'handleUploadUrl' parameter"
        required_handling: >
          Caller MUST always provide handleUploadUrl. This error throws synchronously during
          options validation before any network request is made — it is a programming error,
          not a runtime failure. Add a validation check at component initialization time.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk#client-uploads'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/client.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      # Evidence: dist/client.cjs retrieveClientToken() — if server returns non-2xx, throws
      # BlobError("Failed to retrieve the client token"). Also throws if JSON parse fails.
      - id: blob-upload-token-fetch-failed
        condition: >
          The handleUploadUrl server route returns a non-2xx response (auth failure, server error,
          route not found), or the response body is not valid JSON containing clientToken.
          Common causes: missing authentication middleware on the handleUpload route, route
          misconfiguration, or server returning HTML error page instead of JSON.
        throws: "BlobError: Failed to retrieve the client token"
        required_handling: >
          Caller MUST wrap upload() in try-catch. The token fetch is the first network request
          in the upload flow — if it fails, the upload never starts. Common mistake: the
          handleUpload route is protected by auth middleware that redirects unauthenticated
          requests to /login (HTML, not JSON), causing this error. Ensure the route returns
          JSON even on auth failure.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk#client-uploads'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/client.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      # Evidence: dist/client.cjs extraChecks validates disallowed options
      # Source: BlobError("client/`upload` doesn't allow `addRandomSuffix`, `cacheControlMaxAge`,
      #   `allowOverwrite` or `ifMatch`. Configure these options at the server side...")
      - id: blob-upload-server-only-options-used
        condition: >
          Caller passes addRandomSuffix, cacheControlMaxAge, allowOverwrite, or ifMatch to upload().
          These options are only valid for server-side put(). They must be configured in
          onBeforeGenerateToken on the server side, not in the client-side upload() call.
        throws: "BlobError: client/`upload` doesn't allow `addRandomSuffix`, `cacheControlMaxAge`, `allowOverwrite` or `ifMatch`. Configure these options at the server side when generating client tokens."
        required_handling: >
          Move storage configuration options to the server-side onBeforeGenerateToken callback
          in your handleUpload route. Do not pass them to upload() on the client.
          The client token embeds these settings from the server at token-generation time.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk#client-uploads'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/client.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

      # Evidence: dist/chunk-WCA6LLLU.cjs requestApi() — all storage API errors propagate
      # BlobClientTokenExpiredError thrown when token TTL exceeded (default 1 hour).
      - id: blob-upload-client-token-expired
        condition: >
          The client token has expired (default TTL: 1 hour from generateClientTokenFromReadWriteToken
          call). This happens when: the user keeps the file picker open for more than an hour before
          clicking upload, or when tokens are pre-generated and cached longer than their TTL.
        throws: "BlobClientTokenExpiredError: Client token has expired."
        required_handling: >
          Caller MUST catch BlobClientTokenExpiredError separately from other errors and prompt
          the user to retry (which will generate a fresh token). Do not treat expired token as
          a permanent failure. The BlobClientTokenExpiredError class is exported from
          '@vercel/blob/client' for instanceof checks.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk#client-uploads'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/api.ts'
        severity: warning
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

  - name: generateClientTokenFromReadWriteToken
    import_path: '@vercel/blob/client'
    description: >
      Server-side function that generates a client token from a read-write token. The client token
      is a short-lived credential (default TTL: 1 hour) that grants the browser permission to upload
      to a specific pathname with specific constraints. Must be called from a server environment —
      throws if called from the browser. Used internally by handleUpload(), but also exported for
      manual client token generation workflows.

    postconditions:
      # Evidence: dist/client.cjs generateClientTokenFromReadWriteToken() lines 257-295
      # Confirmed: typeof window !== 'undefined' check throws BlobError("must be called from a server environment")
      - id: blob-generate-client-token-browser-env
        condition: >
          generateClientTokenFromReadWriteToken() is called from a browser environment
          (where typeof window !== 'undefined'). This function requires server-side crypto
          to sign the token payload and must never be called client-side as it would
          expose the BLOB_READ_WRITE_TOKEN to users.
        throws: 'BlobError: "generateClientTokenFromReadWriteToken" must be called from a server environment'
        required_handling: >
          Never call generateClientTokenFromReadWriteToken() in client-side code (React components,
          browser scripts, client-side Next.js pages). It must only be called in server-side code:
          API routes, Server Actions, getServerSideProps, or route handlers in Next.js App Router.
          The read-write token must never be exposed to the browser.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk#client-uploads'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/client.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: SECURITY_RISK
          production_behavior: immediate_exception
          visibility: visible
          user_experience: authentication_failure
          incident_cost_range: high

      # Evidence: dist/client.cjs lines 268-281
      # Confirmed: token.split("_")[3] must exist, else throws BlobError("Invalid `token`...")
      - id: blob-generate-client-token-invalid-token
        condition: >
          The BLOB_READ_WRITE_TOKEN is missing, malformed (doesn't contain the expected storeId
          segment after the 4th underscore), or the token option is passed but invalid.
          The token format is: vercel_blob_rw_<storeId>_<secret>.
        throws: 'BlobError: Invalid `token` parameter (if token option given) or Invalid `BLOB_READ_WRITE_TOKEN`'
        required_handling: >
          Caller MUST wrap generateClientTokenFromReadWriteToken() in try-catch.
          This commonly fails in development when BLOB_READ_WRITE_TOKEN is not pulled via
          `vercel env pull`. Validate the environment variable is set at application startup.
          When this throws inside handleUpload(), the entire upload route fails with 500.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/client.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      # Evidence: dist/client.cjs lines 263-267 — ifMatch + allowOverwrite: false conflict
      # Source confirmed: throws BlobError("ifMatch and allowOverwrite: false are contradictory")
      - id: blob-generate-client-token-conflicting-options
        condition: >
          Both ifMatch and allowOverwrite: false are specified in the token options. These options
          are contradictory: ifMatch implies a conditional overwrite (allowOverwrite must be true),
          but allowOverwrite: false forbids overwrites entirely.
        throws: "BlobError: ifMatch and allowOverwrite: false are contradictory. ifMatch is used for conditional overwrites, which requires allowOverwrite to be true."
        required_handling: >
          When using ifMatch for optimistic concurrency control, either omit allowOverwrite
          (the SDK will auto-set it to true) or explicitly pass allowOverwrite: true.
          Do not combine ifMatch with allowOverwrite: false.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/client.ts'
        severity: warning
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

  - name: createMultipartUploader
    import_path: '@vercel/blob'
    description: >
      Convenience wrapper around the three-phase multipart upload API. Returns an uploader object
      with uploadPart() and complete() methods that share the same upload session (key + uploadId)
      automatically. Simpler than manually calling createMultipartUpload/uploadPart/completeMultipartUpload
      because session state is managed internally. Same error surface as createMultipartUpload() for
      the initial call, plus uploadPart() and complete() errors on method calls.

    postconditions:
      # Evidence: dist/chunk-WCA6LLLU.cjs createCreateMultipartUploaderMethod() — calls createMultipartUpload
      # then returns uploader object. Same error surface as createMultipartUpload for the factory call.
      - id: blob-create-multipart-uploader-no-try-catch
        condition: >
          createMultipartUploader() throws BlobAccessError (auth failure), BlobServiceRateLimited
          (rate limit), BlobServiceNotAvailable (network failure / fetch failed), or BlobUnknownError.
          The initial call creates the upload session — if it fails, no session is created and no
          cleanup is needed.
        throws: BlobAccessError, BlobServiceRateLimited, BlobServiceNotAvailable, or BlobUnknownError.
        required_handling: >
          Caller MUST wrap the createMultipartUploader() call in try-catch.
          The returned uploader object's uploadPart() and complete() methods also throw and must
          each be individually caught. A failed uploadPart() does not automatically abort the session —
          the session remains open until complete() is called or it expires server-side.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/multipart/create-uploader.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      # Evidence: dist/chunk-WCA6LLLU.cjs uploader.uploadPart() — isPlainObject check
      # Source: BlobError("Body must be a string, buffer or stream. You sent a plain JavaScript object...")
      - id: blob-create-multipart-uploader-plain-object-body
        condition: >
          uploader.uploadPart() is called with a plain JavaScript object as the body
          (e.g., { content: 'data' }). The body must be a string, Buffer, ReadableStream,
          File, or Blob. This is a runtime validation error that throws synchronously.
        throws: "BlobError: Body must be a string, buffer or stream. You sent a plain JavaScript object, double check what you're trying to upload."
        required_handling: >
          Ensure the body passed to uploader.uploadPart() is a proper binary or text type.
          Common mistake: passing a JS object that represents file metadata instead of the
          actual file content. Convert to Buffer, Blob, or ReadableStream before uploading.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/multipart/create-uploader.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

  - name: createFolder
    import_path: '@vercel/blob'
    description: >
      Creates a virtual folder in the Vercel Blob store by creating a zero-byte blob with a trailing
      slash pathname. Vercel Blob has no real concept of folders — the file browser on Vercel.com
      displays folders based on trailing slashes in pathnames. The resulting url can be used with del()
      to delete the folder marker. Throws same error types as put().

    postconditions:
      # Evidence: dist/chunk-WCA6LLLU.cjs createFolder() — calls requestApi() via PUT
      # Same error surface as put() — BlobAccessError, BlobStoreSuspendedError, BlobUnknownError, etc.
      - id: blob-create-folder-no-try-catch
        condition: >
          createFolder() throws BlobAccessError (auth failure / missing token),
          BlobStoreSuspendedError (store has been suspended), BlobStoreNotFoundError
          (store in token references deleted store), BlobServiceRateLimited,
          BlobServiceNotAvailable (network failure), or BlobUnknownError.
        throws: BlobAccessError, BlobStoreSuspendedError, BlobStoreNotFoundError, BlobServiceRateLimited, BlobServiceNotAvailable, or BlobUnknownError.
        required_handling: >
          Caller MUST wrap createFolder() in try-catch. Although folder creation is typically
          a one-time setup operation, failures in file browser features must be handled to
          surface errors to users rather than silently creating an incomplete folder hierarchy.
          The returned url should be stored if the folder needs to be deleted later.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/create-folder.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

  - name: issueSignedToken
    import_path: '@vercel/blob'
    description: >
      Server-side function that requests short-lived signed-token material from the Blob control API
      (POST /signed-token). Returns IssuedSignedToken with { delegationToken, clientSigningToken, validUntil }.
      Used together with presignUrl() to obtain a presigned URL for GET/HEAD/PUT/DELETE that the CDN
      verifies without a bearer token. Authenticate with OIDC (VERCEL_OIDC_TOKEN + storeId) or a
      read-write token. Client (browser) tokens are NOT allowed by the server for this operation.
      Introduced in @vercel/blob v2.x as part of the presigned-URL workflow.

    postconditions:
      # Evidence: dist/chunk-HZSIXSMY.cjs:1734 issueSignedToken()
      # Source confirmed: throws BlobError on missing options or invalid validUntil
      - id: blob-issue-signed-token-no-try-catch
        condition: >
          issueSignedToken() throws BlobError on synchronous validation failures (missing options
          object, empty operations array, validUntil that is not a finite integer milliseconds
          timestamp, or validUntil <= now). It also throws BlobAccessError (auth failure),
          BlobStoreSuspendedError, BlobStoreNotFoundError, BlobServiceRateLimited,
          BlobServiceNotAvailable, or BlobUnknownError from the underlying requestApi() call to
          POST /signed-token.
        throws: BlobError, BlobAccessError, BlobStoreSuspendedError, BlobStoreNotFoundError, BlobServiceRateLimited, BlobServiceNotAvailable, or BlobUnknownError.
        required_handling: >
          Caller MUST wrap issueSignedToken() in try-catch. This is the first call in the
          presigned-URL workflow — if it fails, no presigned URL can be issued and the entire
          client upload / signed-URL session is aborted. Sync validation errors (BlobError on
          bad validUntil) are programming errors that should be caught at request-time and
          surfaced as 400 to the client, not as 500. Auth errors should be surfaced as 401/500
          depending on whether the request originated server-side or proxied a client request.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/signed-token.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      # Evidence: dist/chunk-HZSIXSMY.cjs:1725-1732 — assertIssueSignedTokenValidUntilOption()
      # throws BlobError if validUntil is not an integer ms timestamp OR is in the past.
      - id: blob-issue-signed-token-valid-until-in-past
        condition: >
          issueSignedToken() is called with validUntil that is <= Date.now() (a timestamp already
          in the past). This is a synchronous BlobError thrown before any network call. Common
          cause: passing a number-of-seconds (e.g. 3600) instead of an absolute ms timestamp
          (Date.now() + 3600 * 1000), or reusing a cached validUntil after the cache TTL elapsed.
        throws: 'BlobError: `issueSignedToken`: validUntil must be in the future.'
        required_handling: >
          Always compute validUntil as Date.now() + ttlMs, not a duration. Validate the value is
          a future timestamp before calling. If signing tokens are pre-computed, recompute
          validUntil at issuance time, not at scheduling time.
        sources:
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/signed-token.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

  - name: presignUrl
    import_path: '@vercel/blob'
    description: >
      Server-side function that builds a presigned URL for GET / HEAD (blob object host),
      PUT / multipart (control API), or DELETE (DELETE /?pathname=… on the control API).
      Consumes the { delegationToken, clientSigningToken } pair returned from issueSignedToken().
      The CDN verifies the URL signature without requiring a bearer token. HEAD reuses the GET
      URL shape but the canonical signing string includes `operation=head` so a GET-signed URL
      cannot be replayed as HEAD. Introduced in @vercel/blob v2.x.

    postconditions:
      # Evidence: dist/chunk-HZSIXSMY.cjs:1932 presignUrl() + 1840 presign()
      # Confirmed: throws BlobError on missing token fields, invalid delegation payload,
      # pathname mismatch vs scope, expired delegation, or operation not authorized in scope.
      - id: blob-presign-url-no-try-catch
        condition: >
          presignUrl() throws BlobError synchronously for any of: missing clientSigningToken /
          delegationToken from the IssuedSignedToken pair, invalid or unreadable delegationToken
          payload, pathname mismatch (options.pathname does not match the scope encoded in the
          delegation), expired delegation (Date.now() > scope.validUntil), or operation not
          present in scope.operations (e.g. operation: 'put' against a get-only delegation).
        throws: BlobError (sync — clientSigningToken/delegationToken required, invalid delegation payload, pathname mismatch, expired delegation, operation not authorized).
        required_handling: >
          Caller MUST wrap presignUrl() in try-catch. These are all programming errors except
          the expired-delegation case, which is the normal expiry path and must be handled by
          issuing a fresh token via issueSignedToken() and retrying. Returning a 5xx for
          expired-delegation is wrong UX — surface a 400 with "presigned URL expired, retry"
          so the client can re-request a token.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/signed-token.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      # Evidence: dist/chunk-HZSIXSMY.cjs:1858-1862 — scope validUntil expiry check
      - id: blob-presign-url-delegation-expired
        condition: >
          presignUrl() is called with a signedToken whose scope.validUntil has already passed
          (Date.now() > scope.validUntil). This is the dominant expected-failure mode in
          production: signed tokens are short-lived (default 1 hour) and any retry or queued
          job picked up after the TTL window will throw. Treating this as a generic error
          buries the legitimate retry path.
        throws: 'BlobError: The signed delegation has expired; issue a new token first.'
        required_handling: >
          Catch this error explicitly and re-issue a fresh token via issueSignedToken(), then
          retry presignUrl(). Do NOT bubble it as a 5xx to the client — it is an expected
          condition. For background jobs, refresh the token at the start of each batch, not
          at job enqueue time.
        sources:
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/signed-token.ts'
        severity: warning
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low

  - name: handleUploadPresigned
    import_path: '@vercel/blob/client'
    description: >
      Server route helper for presigned client uploads — the counterpart of handleUpload() for
      the presigned-URL flow. Returns a presigned control-plane PUT URL for single-object
      uploads, or a presigned POST URL for /mpu when multipart. Verifies upload-completed
      callbacks with Ed25519 (BLOB_WEBHOOK_PUBLIC_KEY) over x-vercel-signature. Requires a
      getSignedToken callback that returns a token with 'put' privileges. Introduced in
      @vercel/blob v2.x.

    postconditions:
      # Evidence: dist/client.d.ts:452 handleUploadPresigned() — same outer wrapping pattern
      # as handleUpload(). Re-throws errors from getSignedToken and onUploadCompleted callbacks.
      - id: blob-handle-upload-presigned-no-try-catch
        condition: >
          handleUploadPresigned() throws BlobError on missing/invalid request body, signature
          verification failure (BLOB_WEBHOOK_PUBLIC_KEY mismatch on the upload-completed
          callback), or BlobAccessError on missing/invalid token. It also re-throws any error
          thrown by the getSignedToken or onUploadCompleted callbacks. Webhook signature
          mismatch is the dominant failure mode — happens whenever the public key env var
          drifts from the keypair Vercel signs callbacks with.
        throws: BlobError (sync validation, signature verification failure), BlobAccessError (auth failure), or re-thrown errors from getSignedToken / onUploadCompleted callbacks.
        required_handling: >
          The outer route handler MUST wrap handleUploadPresigned() in try-catch and return
          an HTTP error response, not let the exception propagate. Signature-verification
          failures should be returned as 401 (callback rejected) — a 500 here would silently
          fail upload completion notifications. Validation errors from getSignedToken (e.g.
          pathname not allowed for this user) should be returned as 403.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk#client-uploads'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/client.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      # Evidence: dist/client.d.ts:445 — Ed25519 webhook signature verification
      - id: blob-handle-upload-presigned-missing-webhook-public-key
        condition: >
          handleUploadPresigned() is called without webhookPublicKey AND BLOB_WEBHOOK_PUBLIC_KEY
          env var is not set. Upload-completed callbacks cannot be verified — either the
          handler will reject all callbacks (no key configured), or in worst case it will
          accept unsigned callbacks and let an attacker spoof "upload completed" events to
          trigger your onUploadCompleted business logic (e.g. database inserts) without an
          actual upload occurring.
        throws: BlobError on signature verification failure, or silently accepts spoofed callbacks if misconfigured.
        required_handling: >
          Set BLOB_WEBHOOK_PUBLIC_KEY in your Vercel project env vars BEFORE deploying any
          route that uses handleUploadPresigned(). Treat a missing public key as a fatal
          startup error, not a warning. The keypair is generated by Vercel and must be
          fetched once via the dashboard.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk#client-uploads'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: SECURITY_RISK
          production_behavior: silent_failure
          visibility: silent
          user_experience: security_breach
          incident_cost_range: high

  - name: uploadPresigned
    import_path: '@vercel/blob/client'
    description: >
      Client-side upload via the presigned-URL flow. Mirrors upload(), but instead of fetching
      a client token from handleUpload(), it fetches a presigned URL from a handleUploadPresigned()
      route and uploads directly to the presigned URL. Useful when you want a stateless,
      signature-based authorization model without persistent client tokens. Introduced in
      @vercel/blob v2.x.

    postconditions:
      # Evidence: dist/client.d.ts:218 uploadPresigned() — same error surface as upload()
      # (shared infrastructure in createPutMethod + retrieveClientToken-equivalent).
      - id: blob-upload-presigned-no-try-catch
        condition: >
          uploadPresigned() throws BlobError if handleUploadUrl is missing (programming error,
          synchronous), if the presigned-URL fetch from the server route fails (non-2xx or
          non-JSON response), if the presigned URL has expired between fetch and use
          (BlobClientTokenExpiredError-equivalent), or if the storage API rejects the upload
          (BlobAccessError, BlobFileTooLargeError, BlobServiceRateLimited, BlobUnknownError).
        throws: BlobError, BlobAccessError, BlobFileTooLargeError, BlobServiceRateLimited, BlobUnknownError, or BlobClientTokenExpiredError-equivalent.
        required_handling: >
          Caller MUST wrap uploadPresigned() in try-catch. Distinguish expired-presigned-URL
          (retry by re-fetching) from auth-rejected (surface auth error to user) from
          size-too-large (surface file-size error). A single catch-all "upload failed" message
          buries the legitimate retry path for expired URLs.
        sources:
          - 'https://vercel.com/docs/vercel-blob/using-blob-sdk#client-uploads'
          - 'https://github.com/vercel/storage/blob/main/packages/blob/src/client.ts'
        severity: error
        evidence_quality: confirmed
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

notes:
  - All functions require BLOB_READ_WRITE_TOKEN env var (set automatically on Vercel, must be set manually in dev)
  - 'BlobAccessError: missing/invalid token. BlobUnknownError: network/API failures.'
  - 'put() accepts: string, Buffer, ReadableStream, File, Blob'
  - del() accepts single URL string or array of URL strings
  - list() supports prefix, cursor, limit for pagination
  - head() THROWS on not-found (BlobNotFoundError), unlike get() which returns null
  - get() returns null on 404, throws on auth/network errors — callers must check both
  - handleUpload() MUST authenticate user in onBeforeGenerateToken — anonymous uploads otherwise
  - 'Multipart upload: createMultipartUpload → uploadPart (5MB min per part) → completeMultipartUpload'
  - BlobServiceRateLimited includes retryAfter property (seconds) from Retry-After header
  - copy() does NOT preserve contentType or cacheControlMaxAge from source blob
  - upload() is the client-side upload API (browser) — requires handleUploadUrl to fetch a client token
  - generateClientTokenFromReadWriteToken() MUST be called server-side only — never in browser code
  - createMultipartUploader() is a convenience wrapper — manages key+uploadId state internally
  - createFolder() creates a virtual folder (trailing-slash blob) — Vercel Blob has no real folders
  - 'BlobClientTokenExpiredError: token TTL expired (default 1 hour). Retry the upload to get a fresh token.'
  - 'BlobPathnameMismatchError: client token pathname does not match upload() pathname'
  - 'BlobStoreSuspendedError: store suspended by Vercel — contact support'
  - 'issueSignedToken() + presignUrl() are the v2.x presigned-URL flow — server-side, control-plane'
  - 'issueSignedToken() validUntil MUST be an integer ms timestamp in the future (sync BlobError otherwise)'
  - 'presignUrl() throws BlobError if scope.validUntil has expired — re-issue token + retry instead of bubbling 5xx'
  - 'handleUploadPresigned() verifies webhooks with Ed25519 over BLOB_WEBHOOK_PUBLIC_KEY — set in prod env'
  - 'uploadPresigned() is the client-side counterpart to handleUploadPresigned() — distinguish expired vs auth-rejected vs file-too-large failures'
tags:
  - vercel
  - blob
  - storage
  - file-upload
  - error-handling
  - network
  - sdk
related_packages:
  - '@aws-sdk/client-s3'
  - '@aws-sdk/lib-storage'
  - '@google-cloud/storage'
