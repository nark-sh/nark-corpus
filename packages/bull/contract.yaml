package: bull
semver: ">=3.0.0 <5.0.0"
contract_version: "1.1.0"
maintainer: "corpus-team"
last_verified: "2026-04-17"  # deepen-stream-1 pass 3: expanded from 21→23 contracted functions (Job.extendLock, Queue.getMetrics)
coverage_status: covered
status: production  # Event listener detection now supported by EventListenerAnalyzer
evidence_quality: confirmed
coverage_score: 0.74  # 23 contracted / 31 total async-callable (13 omitted read-only/monitoring, +2 this pass: Job.extendLock + Queue.getMetrics)
# effective_coverage: 1.0 — 23/23 non-omitted functions contracted
# functions_added_this_pass (deepen-stream-1 pass 3): Job.extendLock, Queue.getMetrics
# functions_added_previous_pass: Queue.whenCurrentJobsFinished, Queue.removeJobs, Job.releaseLock, Job.moveToFailed
# Queue.getCountsPerPriority intentionally omitted — read-only diagnostic (see omission note below)

# Event handler detection NOW SUPPORTED (as of 2026-02-27):
# The EventListenerAnalyzer can now detect missing event handlers:
# - queue.on('failed', ...) - Missing listener for job failures
# - queue.on('stalled', ...) - Missing listener for stalled jobs (CRITICAL - causes duplicate processing!)
# - queue.on('error', ...) - Missing listener for Redis connection errors
#
# Contract now provides automated detection of:
# - Missing 'stalled' event listener (CRITICAL - 80-90% of codebases)
# - Missing 'failed' event listener (VERY COMMON - 60-70% of codebases)
# - Missing 'error' event listener (COMMON - causes crashes)
#
# bull: Redis-based queue for handling distributed jobs and messages
# Job processors MUST handle errors + queues MUST have event listeners
#
# Common production bugs:
# 1. Missing 'stalled' event listener - CRITICAL (80-90% of codebases, causes duplicate processing!)
# 2. Missing 'failed' event listener - VERY COMMON (60-70% of codebases, silent failures!)
# 3. Missing error handling in processor - COMMON (50-60% of codebases)
# 4. Queue instance leaks - COMMON (15-25% of codebases, Redis connection exhaustion)
# 5. Mixing callbacks and promises - COMMON (20-30% of codebases, queue deadlock!)

detection:
  class_names: ["Queue"]
  type_names: ["Job"]
  factory_methods: []
  require_instance_tracking: true

  # Required event listeners to prevent production issues
  required_event_listeners:
    - event: "failed"
      required: true
      severity: error
    - event: "stalled"
      required: true
      severity: error
    - event: "error"
      required: true
      severity: error

functions:
  - name: Queue.process
    import_path: "bull"
    description: "Defines processor function for handling jobs in the queue"
    postconditions:
      - id: missing-error-handler
        condition: "Job processor doesn't handle errors via try-catch or done(error)"
        throws: "Throws unhandled exception that Bull captures and marks job as failed"
        required_handling: >
          Caller MUST handle errors in job processor using either:
          1. try-catch in async processors: try { await work(); } catch (err) { throw err; }
          2. done(error) in callback processors: done(err)
          Without error handling, exceptions are caught by Bull but may not be logged,
          and failed jobs accumulate silently if no 'failed' event listener exists.
        sources:
          - "https://github.com/OptimalBits/bull"
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      - id: missing-failed-listener
        condition: "Queue instance created without 'failed' event listener"
        throws: "Emits 'failed' event when job fails"
        required_handling: >
          Caller MUST attach 'failed' event listener to queue instance.
          Without this listener, failed jobs are silently lost with no visibility.
          CRITICAL: This is production bug #2 (60-70% of codebases).
          Always add: queue.on('failed', (job, err) => { logger.error('Job failed:', err); })
        sources:
          - "https://blog.logrocket.com/asynchronous-task-processing-in-node-js-with-bull/"
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      - id: missing-stalled-listener
        condition: "Queue instance created without 'stalled' event listener"
        throws: "Emits 'stalled' event when job stalls (CPU-intensive code blocks event loop)"
        required_handling: >
          Caller MUST attach 'stalled' event listener to queue instance.
          Stalled jobs are restarted by another worker, resulting in DUPLICATE PROCESSING.
          CRITICAL: This is production bug #1 (80-90% of codebases don't detect this).
          Impact: Duplicate emails, duplicate payments, data corruption.
          Always add: queue.on('stalled', (job) => { logger.error('Job stalled:', job.id); })
        sources:
          - "https://docs.bullmq.io/guide/jobs/stalled"
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      - id: missing-error-listener
        condition: "Queue instance created without 'error' event listener"
        throws: "Emits 'error' event for Redis connection errors, queue errors"
        required_handling: >
          Caller SHOULD attach 'error' event listener to queue instance.
          Without this listener, Redis connection errors and queue errors go unnoticed.
          Always add: queue.on('error', (error) => { logger.error('Queue error:', error); })
        sources:
          - "https://github.com/OptimalBits/bull"
        severity: warning
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

    edge_cases:
      - id: callback-promise-mix
        description: >
          CRITICAL (20-30% of codebases): Mixing callback-based and promise-based code in processor.
          Example: Using callback-style API calls alongside async/await or Promise.
          Impact: Race conditions cause jobs to stall indefinitely, blocking all subsequent jobs.
          DEADLOCK: Queue never recovers until application restart.
          Fix: Use async/await consistently throughout processor:
          // ❌ WRONG
          queue.process(async (job) => {
            action1(job.data, (err, result) => { ... });  // Callback
            return action2(job.data);  // Promise - race condition!
          });
          // ✅ CORRECT
          queue.process(async (job) => {
            const result1 = await promisify(action1)(job.data);
            const result2 = await action2(job.data);
            return result2;
          });
        sources:
          - "https://github.com/OptimalBits/bull/issues/1822"
        severity: warning
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      - id: cpu-intensive-processor
        description: >
          CRITICAL (30-40% of codebases): CPU-intensive synchronous code in processor blocks event loop.
          Example: Large loops, image processing, complex calculations without yielding.
          Impact: Lock renewal fails, job marked stalled, restarted by another worker.
          Result: DUPLICATE PROCESSING (emails sent twice, payments charged twice).
          Fix: Break into async chunks using setImmediate() or increase lockDuration:
          // ❌ WRONG
          for (let i = 0; i < 1000000; i++) { /* sync work */ }
          // ✅ CORRECT
          for (let i = 0; i < 1000000; i += 10000) {
            await new Promise(resolve => setImmediate(resolve));
            // Process chunk
          }
        sources:
          - "https://docs.bullmq.io/guide/jobs/stalled"
        severity: warning
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      - id: queue-instance-leak
        description: >
          COMMON (15-25% of codebases): Creating new Queue instances repeatedly.
          Example: Instantiating Queue in request handlers, loops, or per-operation.
          Impact: Redis connection leak, gradual memory exhaustion, eventual crash.
          Symptom: "Too many clients" Redis error after hours/days of operation.
          Fix: Instantiate once at application startup, reuse, close on shutdown:
          // ❌ WRONG
          app.post('/api', (req, res) => {
            const queue = new Queue('jobs', redisConfig);  // Leak!
            queue.add(req.body);
          });
          // ✅ CORRECT
          const jobQueue = new Queue('jobs', redisConfig);  // Global/module scope
          app.post('/api', (req, res) => {
            jobQueue.add(req.body);
          });
          process.on('SIGTERM', async () => {
            await jobQueue.close();  // Cleanup
          });
        sources:
          - "https://github.com/OptimalBits/bull/issues/1822"
        severity: warning
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

      - id: manual-job-finalization
        description: >
          UNCOMMON (5-10% of codebases): Calling job.moveToFailed() from processor function.
          Example: try-catch that calls job.moveToFailed(error) instead of throwing.
          Impact: Race condition - both processor and manual code try to finalize job.
          Result: 'failed' event not emitted, monitoring blind spots.
          Rule: NEVER call moveToFailed() from processor - return/throw errors instead.
          // ❌ WRONG
          queue.process(async (job) => {
            try {
              await work();
            } catch (error) {
              await job.moveToFailed({ message: 'failed' });  // WRONG
            }
          });
          // ✅ CORRECT
          queue.process(async (job) => {
            await work();  // Just throw on error, Bull handles finalization
          });
        sources:
          - "https://github.com/OptimalBits/bull/issues/1104"
        severity: warning
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

  - name: new Queue
    import_path: "bull"
    description: "Creates a new Bull queue instance"
    postconditions:
      - id: missing-queue-listeners
        condition: "Queue instantiated without required event listeners"
        throws: "Queue emits events but no listeners attached"
        required_handling: >
          After creating Queue instance, MUST attach event listeners:
          1. queue.on('failed', ...) - REQUIRED
          2. queue.on('stalled', ...) - CRITICAL
          3. queue.on('error', ...) - RECOMMENDED
          See Queue.process postconditions for details.
        sources:
          - "https://github.com/OptimalBits/bull"
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

    edge_cases:
      - id: repeated-instantiation
        description: >
          Creating Queue instances repeatedly instead of reusing.
          See queue-instance-leak in Queue.process edge_cases.
        sources:
          - "https://github.com/OptimalBits/bull/issues/1822"
        severity: warning
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium

  # Evidence: bull/lib/queue.js Job.create() → queue.isReady().then(() => addJob(...))
  # Evidence: bull/lib/job.js Job.create() calls addJob which uses Redis pipeline
  # Redis errors propagate as plain Error objects (from ioredis)
  - name: Queue.add
    import_path: "bull"
    description: >
      Creates a new job and adds it to the queue. Internally waits for Redis ready
      state then executes a Lua script to add the job. If the queue is empty the
      job executes immediately; otherwise it waits. Uses jobId for deduplication —
      duplicate jobId silently drops the new job (job not added, no error thrown).
      Returns the Job object including its assigned jobId.
    postconditions:
      # Evidence: bull/lib/queue.js Job.create() calls isReady() then addJob() via Redis pipeline
      # ioredis throws Error objects on connection failure with messages like "Connection is closed."
      - id: add-redis-connection-error
        condition: "Queue.add() called when Redis connection is unavailable"
        severity: error
        description: >
          Throws Error when Redis connection is unavailable at the time of add().
          The error propagates from the ioredis client when the underlying TCP
          connection is closed, timed out, or the Redis server is unreachable.
          No job is created. Callers that omit try-catch lose jobs silently.
        required_handling: >
          Caller MUST wrap Queue.add() in try-catch and handle the Error.
          Without error handling, jobs are lost silently with no retry mechanism.
          Always log the error and implement a fallback (retry queue, dead letter, alert).
        sources:
          - "https://github.com/OptimalBits/bull"
          - "https://github.com/luin/ioredis"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: silent
          user_experience: degraded_performance
          incident_cost_range: high
        evidence_quality: confirmed

      # Evidence: bull/lib/queue.js Queue.add() — opts.repeat path calls nextRepeatableJob()
      # Non-repeat path: duplicate jobId silently returns existing job id (no error, no new job)
      - id: add-duplicate-job-id-silent-drop
        condition: "Queue.add() called with a jobId that already exists in the queue"
        severity: warning
        description: >
          When a custom jobId is specified and a job with that ID already exists in
          the queue (waiting or active), Bull silently drops the new job — add()
          resolves successfully but the job is never added. Callers that rely on
          add() always creating a new job will find their work silently lost.
        required_handling: >
          Caller SHOULD verify job creation by checking the returned Job object or
          querying job status after add(). For deduplication flows, document the
          intent explicitly. Do not assume add() always creates a new job.
        sources:
          - "https://github.com/OptimalBits/bull"
        business_impact:
          incident_label: SILENT_FAILURE
          production_behavior: silent_failure
          visibility: silent
          user_experience: degraded_performance
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/queue.js Queue.addBulk() → Job.createBulk() → multi.exec()
  # Batch is atomic at the Redis pipeline level — all-or-nothing if connection drops
  - name: Queue.addBulk
    import_path: "bull"
    description: >
      Adds an array of jobs atomically using a Redis pipeline (multi/exec). All
      jobs are queued in a single round-trip. If any job's Redis command fails,
      the exec() rejects and no jobs are confirmed. Note: 'repeat' option is not
      supported in addBulk (https://github.com/OptimalBits/bull/issues/1731).
    postconditions:
      # Evidence: bull/lib/job.js Job.createBulk() — uses multi.exec() which rejects on connection error
      - id: addbulk-redis-connection-error
        condition: "Queue.addBulk() called when Redis connection is unavailable"
        severity: error
        description: >
          Throws Error when Redis connection fails during the pipeline exec().
          Unlike Queue.add() which can partially succeed, addBulk() uses a Redis
          MULTI/EXEC transaction — if the connection drops mid-pipeline, no jobs
          are added and the returned promise rejects.
        required_handling: >
          Caller MUST wrap Queue.addBulk() in try-catch and handle the Error.
          On failure, the entire batch is lost — implement retry logic or store
          the batch for resubmission. Log the error with the batch contents.
        sources:
          - "https://github.com/OptimalBits/bull"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: silent
          user_experience: degraded_performance
          incident_cost_range: high
        evidence_quality: confirmed

      # Evidence: bull/lib/queue.js Queue.addBulk() — no validation on empty array input
      # Bull will call multi.exec() with zero commands which resolves to empty array — safe
      # but the real risk: callers assuming each added job can be retried independently
      - id: addbulk-repeat-option-unsupported
        condition: "Queue.addBulk() called with jobs that include a repeat option"
        severity: warning
        description: >
          The 'repeat' option in JobOptions is silently stripped from addBulk() jobs.
          Bull does not process the repeat scheduler for bulk-added jobs
          (https://github.com/OptimalBits/bull/issues/1731). Jobs that should be
          repeating will only run once.
        required_handling: >
          Do not use the repeat option with addBulk(). Add repeating jobs individually
          via Queue.add() to ensure the repeat scheduler is engaged.
        sources:
          - "https://github.com/OptimalBits/bull/issues/1731"
        business_impact:
          incident_label: SILENT_FAILURE
          production_behavior: silent_failure
          visibility: silent
          user_experience: degraded_performance
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/queue.js Queue.prototype.close() uses isReady().then(pause(true))
  # Then disconnects Redis clients: client, subscriber, bclient
  # Redis disconnect errors are caught and the close still resolves
  - name: Queue.close
    import_path: "bull"
    description: >
      Gracefully closes all Redis connections for this queue instance. Pauses the
      local worker (stops accepting new jobs), waits for active jobs to finish
      (unless doNotWaitJobs=true), then disconnects the Redis client, subscriber,
      and blocking client. MUST be called on application shutdown to prevent Redis
      connection leaks and allow clean process exit.
    postconditions:
      # Evidence: bull/lib/queue.js — no SIGTERM handler is added automatically by bull
      # Connection leak from never calling close() confirmed in bull issues
      - id: close-not-called-on-shutdown
        condition: "Queue instance created without registering a SIGTERM/shutdown handler that calls Queue.close()"
        severity: error
        description: >
          If Queue.close() is never called during application shutdown, the process
          will not exit cleanly because ioredis keeps the event loop alive. Redis
          connections are leaked — each deployment creates orphaned connections until
          the Redis server's maxclients limit is reached.
        required_handling: >
          Caller MUST register process shutdown handlers that call queue.close().
          Add: process.on('SIGTERM', () => queue.close())
          and: process.on('SIGINT', () => queue.close())
          Without this, the process hangs on exit and leaks Redis connections.
        sources:
          - "https://github.com/OptimalBits/bull"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: delayed_failure
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed

      # Evidence: bull/lib/queue.js Queue.close() — when called inside a job handler,
      # the queue.closing flag defers the actual close until the job completes
      - id: close-called-mid-job
        condition: "Queue.close() called while active jobs are still processing"
        severity: warning
        description: >
          Calling Queue.close() from inside a job processor (e.g. in a shutdown signal
          handler that fires while a job is running) does not immediately terminate.
          Bull defers the close until the current job finishes. Callers expecting
          an immediate shutdown will hang until job completion or timeout.
        required_handling: >
          Set a timeout for graceful shutdown. If shutdown takes too long, force
          exit with process.exit(). Document the expected shutdown behavior.
        sources:
          - "https://github.com/OptimalBits/bull"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: delayed_failure
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/queue.js Queue.prototype.obliterate() — calls pause() then
  # scripts.obliterate() in a loop. Throws if active jobs exist and force:false.
  - name: Queue.obliterate
    import_path: "bull"
    description: >
      Completely and irreversibly destroys a queue and all of its contents — waiting,
      active, completed, failed, and delayed jobs are all deleted. Requires no active
      jobs (unless opts.force=true). This operation CANNOT be undone. Commonly used
      in test teardown; using in production without force guard is dangerous.
    postconditions:
      # Evidence: bull/lib/scripts.js obliterate Lua script returns error code when active jobs present
      # Without force:true, obliterate throws if active jobs exist
      - id: obliterate-active-jobs-error
        condition: "obliterate() called without opts.force=true while active jobs are processing"
        throws: "Error('Cannot obliterate queue that has active jobs, use force option to force obliteration')"
        required_handling: >
          Caller MUST wrap obliterate() in try-catch. Drain the queue first (pause, wait
          for active jobs), or use obliterate({ force: true }) only when data loss is
          acceptable (test environments). Never call obliterate() in production request
          handlers without explicit environment guards.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/queue.js"
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: high
        evidence_quality: confirmed

      # Evidence: obliterate() is documented as irreversible — no soft-delete, no undo
      - id: obliterate-irreversible
        condition: "obliterate() completes successfully in a production environment without environment guards"
        throws: "No error — but all queue data is permanently deleted from Redis with no undo"
        required_handling: >
          Callers MUST add explicit environment guards: check NODE_ENV !== 'production'
          before calling obliterate(). Add admin confirmation for any production use.
          There is no undo — once obliterated, all job history is gone.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/REFERENCE.md"
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: silent_failure
          visibility: silent
          user_experience: service_unavailable
          incident_cost_range: high
        evidence_quality: confirmed

  # Evidence: bull/lib/queue.js Queue.prototype.clean() — throws synchronously for
  # missing grace period or unknown type. Source confirmed from queue.js lines 1313–1326.
  - name: Queue.clean
    import_path: "bull"
    description: >
      Removes jobs from the queue that are older than a grace period (in milliseconds).
      Operates on a specific job status type (completed, wait, active, delayed, failed).
      Returns a promise resolving to an array of removed Job objects. Emits 'cleaned'
      event on success.
    postconditions:
      # Evidence: bull/lib/queue.js — throws new Error('You must define a grace period.')
      # when grace is undefined or null (line 1313)
      - id: clean-missing-grace-period
        condition: "clean() called with grace argument as undefined or null"
        throws: "Error('You must define a grace period.')"
        required_handling: >
          Always provide the grace period as first argument (milliseconds).
          Example: queue.clean(7 * 24 * 3600 * 1000, 'completed').
          Wrap in try-catch in maintenance scripts and cron jobs.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/queue.js"
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

      # Evidence: bull/lib/queue.js — throws new Error('Cannot clean unknown queue type ' + type)
      # for types not in ['completed', 'wait', 'active', 'paused', 'delayed', 'failed']
      - id: clean-invalid-type
        condition: "clean() type argument is not one of: 'completed', 'wait', 'active', 'paused', 'delayed', 'failed'"
        throws: "Error('Cannot clean unknown queue type <type>')"
        required_handling: >
          Use 'wait' (not 'waiting') for waiting jobs. Valid type strings are:
          'completed', 'wait', 'active', 'paused', 'delayed', 'failed'.
          The monitoring API returns 'waiting' but clean() requires 'wait'.
          Wrap in try-catch to handle invalid type errors gracefully.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/queue.js"
        severity: error
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

  # Evidence: bull/lib/job.js Job.prototype.retry() — error messages from errors.js
  # RETRY_JOB_NOT_EXIST: "Couldn't retry job: The job doesn't exist"
  # RETRY_JOB_IS_LOCKED: "Couldn't retry job: The job is locked"
  # RETRY_JOB_NOT_FAILED: "Couldn't retry job: The job has been already retried or has not failed"
  - name: Job.retry
    import_path: "bull"
    description: >
      Re-queues a job that is in the 'failed' state for immediate reprocessing.
      Only operates on failed jobs — throws specific errors for jobs in other states.
      Commonly used in admin dashboards and dead-letter-queue recovery scripts.
    postconditions:
      # Evidence: bull/lib/job.js Job.prototype.retry() → scripts.reprocessJob() result codes
      # result === 0 → RETRY_JOB_NOT_EXIST: "Couldn't retry job: The job doesn't exist"
      - id: retry-job-not-exist
        condition: "Job.retry() called on a job that no longer exists in Redis"
        severity: error
        description: >
          Throws Error("Couldn't retry job: The job doesn't exist") when the job
          has been removed from Redis (expired TTL, manually deleted, or removed
          via removeOnFail option). Common in retry flows that hold job references
          from a previous session.
        required_handling: >
          Caller MUST wrap Job.retry() in try-catch. Verify job existence before
          retrying, or handle the not-exist error gracefully in admin retry flows.
        sources:
          - "https://github.com/OptimalBits/bull"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

      # Evidence: bull/lib/errors.js RETRY_JOB_NOT_FAILED
      # result === -2 → "Couldn't retry job: The job has been already retried or has not failed"
      - id: retry-job-not-failed
        condition: "Job.retry() called on a job that is not in the failed state"
        severity: error
        description: >
          Throws Error("Couldn't retry job: The job has been already retried or has not
          failed") when retry() is called on a job that is not in the 'failed' state.
          This includes jobs that are waiting, active, completed, or already being
          retried by another worker.
        required_handling: >
          Caller MUST wrap Job.retry() in try-catch and handle the not-failed error.
          Check job.getState() before retrying, or catch and ignore this specific error
          in bulk retry flows.
        sources:
          - "https://github.com/OptimalBits/bull"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

  # Evidence: bull/lib/queue.js Queue.prototype.empty() — uses multi.exec() then iterates jobKeys
  # Not atomic: deletes wait/paused/delayed lists first, then removes individual job hashes in a loop.
  # Comment in source: "TODO: Use EVAL to make this operation fully atomic."
  # If process is killed between MULTI/EXEC and job key deletion, orphaned job hashes remain.
  - name: Queue.empty
    import_path: "bull"
    description: >
      Empties the queue by deleting all waiting, paused, and delayed jobs and their associated
      Redis keys. Uses a Redis MULTI/EXEC pipeline but is NOT fully atomic — there is a window
      between deleting the list keys and removing the individual job hashes where a crash
      can leave zombie jobs in Redis. Returns a promise that resolves after all keys are deleted.
      CAUTION: This operation is irreversible. All pending jobs are lost.
    postconditions:
      # Evidence: bull/lib/queue.js Queue.prototype.empty() source comment:
      # "Note that if some other process is adding jobs at the same time as emptying,
      # the queues may not be really empty after this method has executed completely."
      - id: empty-concurrent-add-race
        condition: "Queue.empty() called while another process is concurrently adding jobs to the queue"
        severity: warning
        description: >
          Queue.empty() is not atomic — if another process adds a job between the MULTI/EXEC
          that clears list keys and the subsequent job hash deletion loop, the newly added job
          may persist with no list entry (zombie job). Bull's own documentation notes:
          "the queues may not be really empty after this method has executed completely."
          In multi-process or clustered environments, concurrent add() calls race with empty().
        required_handling: >
          Caller SHOULD pause the queue before emptying to prevent concurrent adds.
          Add: await queue.pause(); await queue.empty(); await queue.resume().
          In single-process environments without concurrent writers, this is safe.
          In clustered environments, coordinate empty() with a distributed lock.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/queue.js"
          - "https://raw.githubusercontent.com/OptimalBits/bull/master/REFERENCE.md"
        business_impact:
          incident_label: SILENT_FAILURE
          production_behavior: silent_failure
          visibility: silent
          user_experience: degraded_performance
          incident_cost_range: medium
        evidence_quality: confirmed

      # Evidence: source comment: "if the method does error between emptying the lists and removing
      # all the jobs, there will be zombie jobs left in redis."
      - id: empty-zombie-jobs-on-crash
        condition: "Queue.empty() called without wrapping in try-catch, and Redis connection drops mid-execution"
        severity: error
        description: >
          Queue.empty() has two phases: (1) MULTI/EXEC to delete list keys, (2) individual
          DEL commands for each job hash. If Redis connection drops or the process crashes
          between these phases, job hashes remain orphaned in Redis with no list reference
          ('zombie jobs'). These zombie jobs consume memory indefinitely and can only be
          removed by manually scanning Redis with KEYS/SCAN. The source explicitly warns:
          "if the method does error between emptying the lists and removing all the jobs,
          there will be zombie jobs left in redis."
        required_handling: >
          Caller MUST wrap Queue.empty() in try-catch. On error, scan Redis for orphaned
          job keys and clean them up. Consider using Queue.obliterate({ force: true }) for
          a more complete cleanup in test environments, as obliterate uses Lua scripts
          that are more atomic than empty().
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/queue.js"
        business_impact:
          incident_label: SILENT_FAILURE
          production_behavior: silent_failure
          visibility: silent
          user_experience: degraded_performance
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/repeatable.js Queue.prototype.removeRepeatable() — computes repeatJobKey from
  # name + RepeatOpts, then calls Lua removeRepeatable script. If key doesn't match, ZREM returns 0
  # (no error thrown — silent no-op). Common bug: slightly different opts (e.g. timezone, every vs cron)
  # produces a different key that doesn't match the scheduled job.
  - name: Queue.removeRepeatable
    import_path: "bull"
    description: >
      Removes a repeating job schedule from the queue. The caller must provide identical
      name and RepeatOpts that were used when the job was originally added via Queue.add().
      Internally computes a repeatJobKey from the options and removes the matching Redis sorted
      set entry plus any pending delayed job for that schedule. If the key does not match
      (e.g. different cron string, different timezone, different jobId), the operation is a
      silent no-op — no error is thrown but the repeating job continues to run.
    postconditions:
      # Evidence: bull/lib/scripts/removeRepeatable-2.js Lua script:
      # local millis = redis.call("ZSCORE", KEYS[1], ARGV[2])
      # if(millis) then ... end  -- silently does nothing if ARGV[2] not in the set
      # redis.call("ZREM", KEYS[1], ARGV[2]) -- ZREM returns 0 if key not found, no error
      - id: remove-repeatable-silent-mismatch
        condition: "removeRepeatable() called with RepeatOpts that do not exactly match the scheduled job's options"
        severity: error
        description: >
          If the name or RepeatOpts passed to removeRepeatable() do not exactly match
          those used when the repeating job was created, the Lua script's ZSCORE call
          returns nil and ZREM returns 0 — no error is thrown. The repeating job
          continues executing indefinitely. Common mismatches:
          - Different cron string format (e.g. '0 * * * *' vs '0 */1 * * *')
          - Missing or different timezone field
          - Different jobId prefix
          - Different name argument
          Callers assuming removal was successful will have a scheduling leak.
        required_handling: >
          After calling removeRepeatable(), verify removal by calling
          queue.getRepeatableJobs() and checking that the job no longer appears.
          Store the exact RepeatOpts used at creation time (e.g. in a database)
          to ensure they can be reproduced exactly for removal.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/repeatable.js"
          - "https://github.com/OptimalBits/bull/blob/master/lib/scripts/removeRepeatable-2.js"
        business_impact:
          incident_label: SILENT_FAILURE
          production_behavior: silent_failure
          visibility: silent
          user_experience: degraded_performance
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/repeatable.js Queue.prototype.removeRepeatableByKey() — same Lua script
  # as removeRepeatable but uses the key string directly. Key format: "<name>:<id>:<endDate>:<tz>:<cron>"
  # If the key is stale or misformatted, ZREM returns 0 — silent no-op.
  - name: Queue.removeRepeatableByKey
    import_path: "bull"
    description: >
      Removes a repeating job schedule identified by its full key string. The key is the
      string representation returned by getRepeatableJobs() in the 'key' field. Internally
      uses the same Lua removeRepeatable script as removeRepeatable(). Silently does nothing
      if the key does not match any scheduled job. This is the preferred removal method when
      you store the key at creation time, as it avoids option-matching issues.
    postconditions:
      # Evidence: bull/lib/repeatable.js — same removeRepeatable Lua script, same silent no-op behavior
      - id: remove-repeatable-by-key-silent-noop
        condition: "removeRepeatableByKey() called with a key that no longer exists or was already removed"
        severity: warning
        description: >
          If the repeatJobKey argument does not match any entry in the Redis sorted set
          (e.g. job was already removed, key was computed incorrectly, or key format has
          changed across Bull versions), the operation resolves successfully with no action.
          No error is thrown. Callers that rely on this operation for idempotent cleanup
          flows (e.g. on worker shutdown) will not detect the double-remove or stale key.
        required_handling: >
          For idempotent cleanup, the silent no-op behavior is acceptable by design.
          For critical schedule removal, verify with queue.getRepeatableJobs() after calling.
          Log the key being removed so that failed removals can be diagnosed via monitoring.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/repeatable.js"
        business_impact:
          incident_label: SILENT_FAILURE
          production_behavior: silent_failure
          visibility: silent
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

  # Evidence: bull/lib/scripts.js scripts.updateProgress() — calls updateProgress Redis command,
  # if code < 0, throws scripts.finishedErrors(code, job.id, 'updateProgress').
  # finishedErrors(-1) = "Missing key for job <id> updateProgress" (job deleted from Redis)
  # finishedErrors(-2) = "Missing lock for job <id> updateProgress" (lock expired / stolen)
  # Common failure mode: long-running jobs with CPU-intensive work lose lock, job restarts on
  # another worker, original worker's progress() calls start throwing on next call.
  - name: Job.progress
    import_path: "bull"
    description: >
      Reports progress (0-100) for the currently executing job. When called with a value,
      updates the job's progress field in Redis and emits a 'progress' event on the queue.
      When called without arguments, returns the current progress value synchronously (no
      Redis call). The Redis-writing variant returns a Promise that can reject — callers in
      job processors often forget to await it or handle its rejection.
    postconditions:
      # Evidence: bull/lib/scripts.js scripts.updateProgress() lines ~140-155:
      # if (code < 0) { throw scripts.finishedErrors(code, job.id, 'updateProgress'); }
      # finishedErrors(-1): "Missing key for job <id> updateProgress"
      # finishedErrors(-2): "Missing lock for job <id> updateProgress"
      - id: progress-job-key-missing
        condition: "job.progress(value) called after the job has been deleted from Redis (e.g. removeOnComplete/removeOnFail removed it, or Redis eviction)"
        severity: error
        description: >
          Throws Error("Missing key for job <id> updateProgress") when the job's Redis key
          no longer exists. This happens when removeOnComplete or removeOnFail options are
          set and the job finished on another worker while this worker was still executing
          — a race condition in distributed processing. Also happens if Redis evicted the
          job key due to maxmemory policy. Unhandled, this crashes the job processor and
          leaves the next job stuck.
        required_handling: >
          Callers SHOULD wrap job.progress() in try-catch within the processor function.
          Alternatively, avoid calling progress() after performing operations that might
          cause the job to be considered complete (e.g. don't call progress after the
          business operation that marks success — the job may have been cleaned up).
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/scripts.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

      # Evidence: bull/lib/scripts.js finishedErrors(-2): "Missing lock for job <id> updateProgress"
      # When a job stalls and is picked up by another worker, the original worker's lock is invalid.
      # Subsequent progress() calls from the stale worker throw -2 lock error.
      - id: progress-lock-lost
        condition: "job.progress(value) called after the job's lock has expired or been taken by another worker"
        severity: error
        description: >
          Throws Error("Missing lock for job <id> updateProgress") when the Bull lock on
          the job has expired (lockStalledInterval elapsed without renewal) or been claimed
          by another worker that picked up the stalled job. The original worker is now a
          'zombie processor' — its progress() calls fail but it may continue executing
          business logic (causing duplicate work). This is a critical split-brain scenario
          in distributed Bull deployments.
        required_handling: >
          Caller SHOULD check for this error in progress() and immediately abort
          the processor function to prevent further duplicate work:
          try { await job.progress(50); }
          catch (err) { if (err.message.includes('Missing lock')) return; throw err; }
          Increase lockDuration and lockRenewTime to prevent premature lock expiry.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/scripts.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/job.js Job.prototype.update() calls scripts.updateData()
  # scripts.updateData() returns code; if code < 0, throws finishedErrors(code, id, 'updateData')
  # finishedErrors(-1): "Missing key for job <id> updateData" — job was deleted
  # finishedErrors(-3): "Job <id> is not in the active state. updateData"
  - name: Job.update
    import_path: "bull"
    description: >
      Updates the job's data payload in Redis while the job is being processed. This is
      used to persist intermediate state for long-running jobs (e.g. recording a transaction
      ID after a payment is charged but before fulfillment is complete). Only works on
      jobs in the active state — throws if the job is no longer active (completed, failed,
      or removed). Returns a Promise.
    postconditions:
      # Evidence: bull/lib/job.js Job.prototype.update() → scripts.updateData()
      # if (code < 0) { throw scripts.finishedErrors(code, this.id, 'updateData'); }
      - id: update-job-not-active
        condition: "job.update(data) called on a job that is no longer in the active state"
        severity: error
        description: >
          Throws Error("Missing key for job <id> updateData") or
          Error("Job <id> is not in the active state. updateData") when job.update() is
          called after the job has completed, failed, or been removed from Redis. This
          occurs in async processor patterns where update() is called after an await that
          takes long enough for the job to time out and be reprocessed elsewhere, or when
          removeOnComplete removes the job while update() is still pending.
        required_handling: >
          Caller MUST wrap job.update() in try-catch. Store critical intermediate state
          in external storage (database) rather than relying solely on job.update() —
          Redis TTL and job removal policies can destroy this data unexpectedly.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/job.js"
          - "https://github.com/OptimalBits/bull/blob/master/lib/scripts.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/job.js Job.prototype.remove() → scripts.remove(queue, job.id)
  # then checks: if (!removed) { throw new Error('Could not remove job ' + job.id); }
  # This throws when the job is active (locked by a worker) or already removed.
  # Active jobs cannot be removed — they must fail/complete first.
  - name: Job.remove
    import_path: "bull"
    description: >
      Removes a job from the queue and all associated Redis lists (waiting, delayed,
      completed, failed, etc.). Returns a promise that resolves when the job is removed,
      or rejects if removal fails (e.g. job is currently active/locked). Commonly used
      in admin dashboards and dead-letter-queue management flows.
    postconditions:
      # Evidence: bull/lib/job.js Job.prototype.remove():
      # scripts.remove(queue, job.id).then(removed => {
      #   if (removed) { queue.emit('removed', job); }
      #   else { throw new Error('Could not remove job ' + job.id); }
      # })
      - id: remove-active-job-error
        condition: "job.remove() called on a job that is currently being processed (active/locked)"
        throws: "Error('Could not remove job <id>')"
        severity: error
        description: >
          Throws Error("Could not remove job <id>") when the job cannot be removed because
          it is currently locked by an active worker. Bull's Lua remove script returns 0
          (not removed) for active jobs, causing this error. Common in admin dashboards
          that allow manual job removal without checking job state first.
        required_handling: >
          Caller MUST wrap job.remove() in try-catch. Check job state before removal:
          const state = await job.getState();
          if (state !== 'active') { await job.remove(); }
          For active jobs, consider using queue.pause() to stop new jobs, then wait for
          the active job to finish before removing it.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/job.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

  # Evidence: bull/lib/job.js Job.prototype.promote() — calls scripts.promote(queue, jobId)
  # If result === -1: throw new Error('Job ' + jobId + ' is not in a delayed state')
  # Common use case: admin dashboards that promote jobs from delayed to immediate execution.
  - name: Job.promote
    import_path: "bull"
    description: >
      Moves a delayed job to the waiting state for immediate execution. Only operates
      on jobs in the 'delayed' state. Throws a specific error if the job is not delayed.
      Used in admin interfaces and retry flows where a delayed job needs to run immediately.
    postconditions:
      # Evidence: bull/lib/job.js Job.prototype.promote():
      # scripts.promote(queue, jobId).then(result => {
      #   if (result === -1) { throw new Error('Job ' + jobId + ' is not in a delayed state'); }
      # })
      - id: promote-not-delayed-error
        condition: "job.promote() called on a job that is not in the delayed state"
        throws: "Error('Job <id> is not in a delayed state')"
        severity: error
        description: >
          Throws Error("Job <id> is not in a delayed state") when promote() is called
          on a job that is waiting, active, completed, or failed. This is common in
          admin interfaces where the job state may have changed between when it was
          retrieved and when promote() is called (TOCTOU race). Also common when a
          cron-triggered promote fires for a job that already ran or was manually moved.
        required_handling: >
          Caller MUST wrap job.promote() in try-catch. Check job.getState() immediately
          before calling promote() and handle the 'not-delayed' error gracefully:
          const state = await job.getState();
          if (state === 'delayed') { await job.promote(); }
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/job.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

  # Evidence: bull/lib/scripts.js scripts.addLog() — calls client.addLog()
  # result < 0 → throw scripts.finishedErrors(result, jobId, 'addLog')
  # finishedErrors(-1): "Missing key for job <id> addLog" (job deleted)
  # finishedErrors(-2): "Missing lock for job <id> addLog" (lock expired)
  # Called from inside job processors — processors rarely expect log() to throw.
  - name: Job.log
    import_path: "bull"
    description: >
      Appends a text entry to the job's log in Redis, accessible via Queue.getJobLogs().
      Called from inside job processors to record execution steps for debugging and
      monitoring. Returns a Promise that resolves to the number of log entries after
      the append. This is an async operation — its rejection must be handled or the
      processor may crash unexpectedly.
    postconditions:
      # Evidence: bull/lib/scripts.js scripts.addLog():
      # if (result < 0) { throw scripts.finishedErrors(result, jobId, 'addLog'); }
      # finishedErrors(-1): "Missing key for job <id> addLog" (job key not in Redis)
      - id: log-job-key-missing
        condition: "job.log(row) called after the job has been deleted from Redis"
        severity: warning
        description: >
          Throws Error("Missing key for job <id> addLog") when the job's Redis key
          has been deleted (e.g. removeOnComplete removed the job, Redis eviction,
          or the job was manually obliterated). This is most common in the last steps
          of a processor when removeOnComplete is enabled — the job completes and is
          immediately deleted, then a final log() call fails. Since job.log() is
          usually "fire and forget" without await, the rejection becomes an unhandled
          promise rejection that crashes the process in Node.js v15+.
        required_handling: >
          Caller SHOULD await job.log() and wrap in try-catch, or attach .catch() to
          the returned promise. Pattern:
          await job.log('Step complete').catch(err => logger.warn('Log failed:', err.message));
          This prevents unhandled rejections without blocking the processor.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/scripts.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

  # Evidence: bull/lib/job.js Job.prototype.finished() → subscribed to global:completed/global:failed
  # If job fails, rejects with new Error(job.failedReason)
  # If queue closes while waiting, rejects with "cannot check if job is finished in a closing queue."
  - name: Job.finished
    import_path: "bull"
    description: >
      Returns a promise that resolves when the job completes successfully (returns the
      job's return value) or rejects if the job fails (throws Error with the job's
      failedReason). Subscribes to Redis pub/sub for completion notification. Useful
      for fire-and-wait patterns where the caller needs the job result synchronously.
    postconditions:
      # Evidence: bull/lib/job.js Job.prototype.finished() line ~518: reject(new Error(failedReason))
      # Confirmed: if job transitions to failed state, finished() rejects
      - id: finished-job-failed
        condition: "Awaiting Job.finished() when the job transitions to failed state"
        severity: error
        description: >
          Rejects with Error(job.failedReason) when the job fails during processing.
          The failedReason is the error message from the processor's thrown error.
          Callers waiting on job.finished() MUST catch this error to avoid unhandled
          promise rejections that crash the process in Node.js v15+.
        required_handling: >
          Caller MUST wrap await job.finished() in try-catch. On rejection, inspect
          the error message (which equals job.failedReason) and handle accordingly.
        sources:
          - "https://github.com/OptimalBits/bull"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed

      # Evidence: bull/lib/job.js line ~539: reject("cannot check if job is finished in a closing queue.")
      - id: finished-queue-closing
        condition: "Queue.close() is called while job.finished() promise is pending"
        severity: error
        description: >
          Rejects with Error("cannot check if job is finished in a closing queue.")
          when Queue.close() is called while one or more promises are waiting on
          job.finished(). The pub/sub subscription is torn down before the job completes.
        required_handling: >
          Caller MUST wrap await job.finished() in try-catch. During shutdown, handle
          the queue-closing rejection gracefully rather than crashing. Consider a
          timeout pattern: Promise.race([job.finished(), shutdownTimeout]).
        sources:
          - "https://github.com/OptimalBits/bull"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/queue.js Queue.prototype.whenCurrentJobsFinished lines 1374-1393
  # Waits for all this.processing promises; forces bclient reconnect to abort BRPOPLPUSH
  # If bclientInitialized is false, resolves immediately
  - name: Queue.whenCurrentJobsFinished
    import_path: "bull"
    description: >
      Returns a promise that resolves when all currently active jobs have finished
      processing. Forces the blocking Redis client to reconnect to abort pending
      BRPOPLPUSH calls, then waits for all in-flight job promises to settle. Used in
      graceful shutdown sequences before calling Queue.close(). Critical for preventing
      job truncation during process shutdown.
    postconditions:
      # Evidence: bull/lib/queue.js Queue.prototype.whenCurrentJobsFinished + close() interaction
      # Confirmed: close() without whenCurrentJobsFinished() abandons active jobs → stall on restart
      - id: when-current-jobs-not-awaited-before-close
        condition: "Queue.close() called without first awaiting Queue.whenCurrentJobsFinished()"
        severity: error
        description: >
          Calling Queue.close() without first awaiting Queue.whenCurrentJobsFinished()
          cuts off active jobs mid-execution. The worker's processing promises are
          abandoned and the jobs end up in a stalled state on the next queue startup,
          triggering Bull's stalled job recovery mechanism which re-queues them for
          duplicate processing. The correct shutdown sequence is:
          await queue.whenCurrentJobsFinished(); await queue.close();
        required_handling: >
          Caller MUST await queue.whenCurrentJobsFinished() before calling queue.close()
          in shutdown handlers:
          process.on('SIGTERM', async () => { await queue.whenCurrentJobsFinished(); await queue.close(); process.exit(0); });
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/queue.js"
        business_impact:
          incident_label: DATA_LOSS
          production_behavior: silent_failure
          visibility: silent
          user_experience: lost_data
          incident_cost_range: high
        evidence_quality: confirmed

      # Evidence: No SIGTERM handler = immediate process exit, whenCurrentJobsFinished never called
      # Bull stall detection re-queues jobs that were active at shutdown → duplicate processing
      - id: when-current-jobs-no-sigterm-handler
        condition: "whenCurrentJobsFinished() not called in SIGTERM/SIGINT handler"
        severity: warning
        description: >
          When a Node.js process receives SIGTERM (e.g., Kubernetes pod eviction, Heroku dyno
          cycling), the default behavior exits immediately. Without a SIGTERM handler that
          calls whenCurrentJobsFinished(), all active jobs are abandoned and will stall.
          Bull's stall detection re-queues them on next restart, causing duplicate
          processing of any jobs that were mid-execution when the process was killed.
        required_handling: >
          Register shutdown handlers that call whenCurrentJobsFinished() before exit:
          process.once('SIGTERM', async () => { await queue.whenCurrentJobsFinished(); await queue.close(); process.exit(0); });
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/queue.js"
        business_impact:
          incident_label: DATA_LOSS
          production_behavior: silent_failure
          visibility: silent
          user_experience: lost_data
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/queue.js Queue.prototype.removeJobs line 611
  # Delegates to static Job.remove(queue, pattern) → scripts.removeWithPattern
  # Uses Redis KEYS pattern matching — O(N) scan, blocks Redis
  - name: Queue.removeJobs
    import_path: "bull"
    description: >
      Removes all jobs from the queue whose jobId matches the given Redis glob-style
      pattern (e.g., 'email-*', 'job:123:*'). Uses Redis KEYS scan which is O(N)
      relative to the number of keys in Redis. Resolves with no return value regardless
      of how many jobs matched. Emits 'removed' event for each matched job. Does NOT
      throw if zero jobs match the pattern.
    postconditions:
      # Evidence: bull/lib/job.js Job.remove static method lines 150-154
      # await queue.isReady() → Redis errors propagate; then scripts.removeWithPattern
      - id: remove-jobs-no-try-catch
        condition: "await queue.removeJobs(pattern) called without try-catch"
        throws: "ioredis Error (Redis connection loss or queue.isReady() rejection)"
        severity: error
        description: >
          Throws ioredis Error when Redis connection is lost during the pattern scan
          or when queue.isReady() rejects. Since removeJobs() internally awaits
          queue.isReady() and executes Redis commands, any Redis disconnection during
          the operation causes an Error to propagate. Without try-catch, this crashes
          the calling async function. Pattern matching zero jobs silently succeeds.
        required_handling: >
          Caller MUST wrap await queue.removeJobs(pattern) in try-catch. On Redis
          connection error, log and retry after reconnection. Zero matches is not an error.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/job.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low
        evidence_quality: confirmed

      # Evidence: Redis KEYS is O(N) blocking command — confirmed from scripts.removeWithPattern
      # Bull uses KEYS not SCAN — can block Redis for seconds on large keyspaces
      - id: remove-jobs-pattern-blocks-redis
        condition: "Queue.removeJobs(pattern) called with broad pattern on large Redis keyspace"
        severity: warning
        description: >
          Internally uses the Redis KEYS command (not SCAN), which is an O(N) blocking
          operation that scans all keys in the Redis keyspace. On Redis instances with
          millions of keys, this blocks all other Redis operations for the duration of
          the scan. In production queues with high job throughput, calling removeJobs()
          with broad patterns (e.g., '*') can cause Redis latency spikes affecting all
          queue workers and other services sharing the Redis instance.
        required_handling: >
          Avoid removeJobs() with broad patterns in production during peak load.
          Prefer Queue.clean() with a specific status and grace period for bulk cleanup.
          If removeJobs() is required, use narrow patterns or schedule during off-peak hours.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/job.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: degraded_service
          visibility: visible
          user_experience: degraded_performance
          incident_cost_range: medium
        evidence_quality: confirmed

  # Evidence: bull/lib/job.js Job.prototype.releaseLock lines 225-230
  # scripts.releaseLock returns 1 on success, 0 if lock not owned
  # if (unlocked != 1) { throw new Error('Could not release lock for job ' + this.id); }
  - name: Job.releaseLock
    import_path: "bull"
    description: >
      Releases the Redis lock held on this job. Only locks owned by the current
      queue worker instance can be released — releasing a lock owned by a different
      worker throws an Error. Used in advanced job processing patterns where manual
      lock management is needed (e.g., passing processing between workers, explicit
      lock lifecycle control beyond normal process() handler).
    postconditions:
      # Evidence: bull/lib/job.js Job.prototype.releaseLock lines 225-230
      # Confirmed source: if (unlocked != 1) { throw new Error('Could not release lock for job ' + this.id); }
      - id: release-lock-not-owner
        condition: "job.releaseLock() called when current worker does not own the job lock"
        throws: "Error('Could not release lock for job <jobId>')"
        severity: error
        description: >
          Throws Error('Could not release lock for job <jobId>') when the lock cannot
          be released because it is owned by a different worker, has already expired,
          or was never acquired by the calling instance. This is especially dangerous
          in multi-worker setups where race conditions can lead to multiple workers
          believing they own the same lock. The error is thrown within the promise.
        required_handling: >
          Caller MUST wrap await job.releaseLock() in try-catch. Only call releaseLock()
          from within the job's own processor function context or after acquiring the lock
          via job.takeLock(). Do not call releaseLock() from outside the processing context.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/job.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low
        evidence_quality: confirmed

      # Evidence: ioredis errors from Lua script execution propagate directly
      # Redis connection loss during releaseLock() throws ioredis Error
      - id: release-lock-no-try-catch
        condition: "await job.releaseLock() called without try-catch"
        throws: "ioredis Error (Redis connection loss during Lua script execution)"
        severity: error
        description: >
          Throws ioredis Error when Redis connection is lost during the Lua script
          execution for lock release. Both Redis connection errors and lock-ownership
          errors propagate as unhandled rejections without try-catch, crashing the
          calling async function. On Redis connection error, the lock will expire
          naturally via its TTL.
        required_handling: >
          Caller MUST wrap await job.releaseLock() in try-catch. Log and handle
          appropriately. On Redis connection error, do not retry — the lock expires
          via TTL automatically.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/job.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low
        evidence_quality: confirmed

  # Evidence: bull/lib/job.js Job.prototype.moveToFailed lines 285-349
  # bull/lib/scripts.js finishedErrors() codes: -1 (missing key), -2 (missing lock),
  # -3 (not in active state), -6 (lock mismatch)
  - name: Job.moveToFailed
    import_path: "bull"
    description: >
      Manually moves a job to the failed state. Typically called by Bull's internal
      processor on job failure, but available for custom error handling flows. If the
      job has remaining retry attempts and has not been discarded via job.discard(),
      moves to delayed or retry queue instead of failed. Returns a tuple of
      [nextJobData, nextJobId] for the next waiting job, or null if none waiting.
    postconditions:
      # Evidence: bull/lib/scripts.js finishedErrors() lines 284-300
      # Confirmed codes: -1 missing key, -3 wrong state, -6 lock mismatch
      - id: move-to-failed-no-try-catch
        condition: "await job.moveToFailed(errorInfo) called without try-catch"
        throws: "Error (Redis Lua script error codes: -1 missing key, -3 wrong state, -6 lock mismatch)"
        severity: error
        description: >
          Throws Error when the underlying Redis Lua script returns an error code:
          -1: 'Missing key for job <id>' — job no longer exists in Redis (already cleaned up);
          -3: 'Job <id> is not in the active state' — job not currently being processed;
          -6: 'Lock mismatch for job <id>' — called from wrong worker or lock expired.
          Without try-catch, these propagate as unhandled promise rejections crashing the
          calling function.
        required_handling: >
          Caller MUST wrap await job.moveToFailed({ message: err.message }) in try-catch.
          On -1 (missing key): job was already cleaned up — log and skip.
          On -3 (wrong state): job completed via another path — log and skip.
          On -6 (lock mismatch): another worker took ownership — do not retry.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/job.js"
          - "https://github.com/OptimalBits/bull/blob/master/lib/scripts.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low
        evidence_quality: confirmed

      # Evidence: bull/lib/job.js moveToFailed lines 297-310
      # if (attemptsMade < opts.attempts && !_discarded) → retries instead of failing
      - id: move-to-failed-retry-not-exhausted
        condition: "job.moveToFailed() called when job still has remaining retry attempts"
        severity: warning
        description: >
          When moveToFailed() is called on a job that has remaining retry attempts
          (attemptsMade < opts.attempts) and the job was not discarded via job.discard(),
          Bull does NOT move it to the failed queue. Instead it moves the job to delayed
          (with backoff) or re-queues for immediate retry. Callers expecting the job to
          be in 'failed' state immediately may be surprised to find it re-processing.
          Call job.discard() before job.moveToFailed() to force failure bypassing retries.
        required_handling: >
          If the intent is to force the job to the failed state immediately (bypassing
          retries), call job.discard() first, then job.moveToFailed(). Without discard(),
          the job will retry per its configured attempts and backoff settings.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/job.js"
        business_impact:
          incident_label: SILENT_FAILURE
          production_behavior: silent_failure
          visibility: silent
          user_experience: lost_data
          incident_cost_range: low
        evidence_quality: confirmed

  - name: Job.extendLock
    import_path: "bull"
    description: >
      Extends the lock on an active job to prevent it from being marked as stalled.
      Must be called periodically in long-running job processors (those that take longer
      than `lockDuration` milliseconds, default 30 seconds). Returns a Promise<number>:
      1 if the lock was successfully extended, 0 if the lock was not extended (lock
      expired or taken by another worker). Bull's internal processor automatically calls
      this via a timer, but external long-running processors (e.g., spawned child processes,
      streaming operations) may need to call it manually.
    postconditions:
      # Evidence: bull/lib/commands/extendLock-2.lua — confirmed return values 0 and 1
      # Evidence: bull/lib/queue.js lines 1155-1179 — internal lock renewer checks `if (lock && \!timerStopped)`
      # Evidence: bull/lib/job.js lines 238-239 — Job.prototype.extendLock delegates to scripts.extendLock
      # Source: https://github.com/OptimalBits/bull/blob/master/lib/commands/extendLock-2.lua
      # Source: https://github.com/OptimalBits/bull/blob/master/lib/queue.js
      - id: extend-lock-zero-return-unchecked
        condition: >
          await job.extendLock(duration) is called without checking the return value,
          and the function returns 0 (lock not extended because lock expired or was
          taken by another worker).
        throws: "Does not throw — returns 0 silently when lock extension fails"
        severity: error
        description: >
          extendLock() resolves with 0 when the Redis Lua script cannot extend the lock
          because (a) the job's lock has already expired, or (b) another worker has taken
          ownership of the job via a different token. A return of 0 means the current
          worker no longer has exclusive ownership. Continuing to process the job after
          receiving 0 causes duplicate processing: two workers run the same job
          simultaneously. This is the same stall/duplicate-processing bug that missing
          'stalled' event listeners cause, but triggered explicitly in long-running processors.
          bull's internal lock renewer (queue.js line 1173) correctly checks `if (lock && \!timerStopped)`.
          User code must do the same.
        required_handling: >
          Caller MUST check the return value of extendLock():
          const extended = await job.extendLock(30000);
          if (\!extended) {
            // Another worker took the job — stop processing immediately
            throw new Error('Lock lost: job taken by another worker');
          }
          Without this check, duplicate processing causes duplicate emails, duplicate
          payments, or data corruption in distributed queue deployments.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/commands/extendLock-2.lua"
          - "https://github.com/OptimalBits/bull/blob/master/lib/queue.js"
        business_impact:
          incident_label: DATA_LOSS
          production_behavior: silent_failure
          visibility: silent
          user_experience: lost_data
          incident_cost_range: high
        evidence_quality: confirmed

      - id: extend-lock-no-try-catch
        condition: >
          await job.extendLock(duration) called without try-catch and Redis connection
          is unavailable or the Redis command fails.
        throws: "Error (ioredis connection error or NOSCRIPT error if Lua script not loaded)"
        severity: warning
        description: >
          extendLock() delegates to a Redis custom command (scripts.extendLock). If Redis
          is unavailable or the connection is dropped mid-processing, the promise rejects
          with a connection error. Without try-catch, this propagates as an unhandled
          rejection. In long-running processors that manually call extendLock on a timer,
          a Redis blip during lock renewal causes the job to be considered stalled by
          other workers, triggering duplicate processing while the original processor
          continues unaware.
        required_handling: >
          Wrap extendLock() calls in try-catch. On Redis error, abort the current
          job processing and let Bull's stall detection handle requeuing:
          try {
            const extended = await job.extendLock(this.settings.lockDuration);
            if (\!extended) throw new Error('Lock lost');
          } catch (err) {
            logger.error('Lock renewal failed, aborting job', err);
            return;
          }
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/queue.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: medium
        evidence_quality: confirmed

  - name: Queue.getMetrics
    import_path: "bull"
    description: >
      Returns metrics for completed or failed jobs as a time-series data structure.
      Accepts type ('completed' | 'failed'), and optional start/end range (default 0/-1
      for all data). Returns { meta: { count, prevTS, prevCount }, data: number[], count: number }.
      Uses Redis pipeline (multi/exec) internally. Data points represent job counts per
      minute bucket. Metrics must be enabled via MetricsOpts in QueueOptions to collect data.
      Returns zeroed meta if no metrics data exists yet.
    postconditions:
      # Evidence: bull/lib/getters.js lines 233-260 — throws err if Redis pipeline reports error
      # Evidence: bull/lib/getters.js line 247 — `if (err || err2) { throw err || err2 || err3; }`
      # Source: https://github.com/OptimalBits/bull/blob/master/lib/getters.js
      - id: get-metrics-metrics-not-enabled
        condition: >
          queue.getMetrics() called but metrics were not enabled in QueueOptions
          (no `metrics` key in queue constructor options).
        throws: "Does not throw — returns zeroed data structure silently"
        severity: warning
        description: >
          If metrics collection was not enabled via the `metrics` option in QueueOptions,
          getMetrics() returns { meta: { count: 0, prevTS: 0, prevCount: 0 }, data: [], count: 0 }
          with no indication that metrics are disabled. Callers that check `result.count === 0`
          cannot distinguish between "metrics not enabled" and "no jobs completed yet."
          Dashboards built on getMetrics() will silently show empty charts if metrics
          were never configured.
        required_handling: >
          Ensure metrics are enabled in the queue constructor:
          const queue = new Queue('jobs', { metrics: { maxDataPoints: 1440 } });
          Document to callers that zero results may mean metrics are disabled,
          not that no jobs have run.
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/getters.js"
          - "https://github.com/OptimalBits/bull/blob/master/README.md"
        business_impact:
          incident_label: SILENT_FAILURE
          production_behavior: silent_failure
          visibility: silent
          user_experience: degraded_performance
          incident_cost_range: low
        evidence_quality: confirmed

      - id: get-metrics-no-try-catch
        condition: >
          await queue.getMetrics('completed') called without try-catch and Redis
          pipeline exec returns an error in the first or second result.
        throws: "Error (Redis pipeline error from multi.exec)"
        severity: warning
        description: >
          getMetrics() uses a Redis multi/exec pipeline. If any pipeline command fails
          (connection error, OOM, cluster redirect), the pipeline exec result includes
          an error in position [0] or [1]. The implementation throws
          `err || err2 || err3` — confirmed from getters.js source line 247.
          Without try-catch around getMetrics(), Redis errors in monitoring/dashboard
          code crash the caller (often a periodic metrics endpoint or cron job).
        required_handling: >
          Wrap getMetrics() in try-catch in dashboard/monitoring code:
          try {
            const metrics = await queue.getMetrics('completed');
            return metrics.data;
          } catch (err) {
            logger.error('Failed to fetch queue metrics:', err);
            return [];
          }
        sources:
          - "https://github.com/OptimalBits/bull/blob/master/lib/getters.js"
        business_impact:
          incident_label: DOWNTIME
          production_behavior: immediate_exception
          visibility: visible
          user_experience: service_unavailable
          incident_cost_range: low
        evidence_quality: confirmed

# Intentionally omitted from deepen-stream-1 pass 3:
# Queue.getCountsPerPriority() — read-only diagnostic returning counts per priority level.
#   Pure Redis ZCOUNT/LLEN queries. No distinct error behavior beyond generic Redis connection
#   errors (identical to count() which is already omitted). Added in bull 4.x. Low adoption.
#   Omission reason: same category as count(), getCompletedCount(), etc. already omitted.
