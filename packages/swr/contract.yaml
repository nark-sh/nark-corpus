package: "swr"
semver: ">=0.0.0"
contract_version: "1.0.0"
maintainer: "nark-sh"
last_verified: "2026-06-12"
coverage_status: "out-of-scope"
skip_reason: "low-callsite"
researched_at: "2026-04-02"
coverage_rationale: |
  React hook pattern (useSWR returns {data, error}) — error handling is via return value field destructuring, not try-catch around async calls. Current scanner architecture detects missing try-catch around awaited calls; detecting missing error field destructuring requires significant scanner changes. Scanner concern queued.
