package: "@apollo/client"
semver: ">=0.0.0"
contract_version: "1.0.0"
maintainer: "nark-sh"
last_verified: "2026-06-12"
coverage_status: "out-of-scope"
skip_reason: "low-callsite"
researched_at: "2026-04-02"
coverage_rationale: |
  Primary usage is React hooks (useQuery/useMutation) which return {error} state rather than throwing. Hook pattern (90%+ of callsites) does not match behavioral contract try-catch detection model. Imperative client.query() calls are a minority pattern.
