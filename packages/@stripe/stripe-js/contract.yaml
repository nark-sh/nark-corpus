package: "@stripe/stripe-js"
semver: ">=0.0.0"
contract_version: "1.0.0"
maintainer: "nark-sh"
last_verified: "2026-06-12"
coverage_status: "out-of-scope"
skip_reason: "low-callsite"
researched_at: "2026-04-02"
coverage_rationale: |
  Browser-side Stripe.js loader. loadStripe() returns null on failure (no throws). Config-only initialization pattern — no behavioral contracts to enforce. Server-side stripe package already in corpus.
