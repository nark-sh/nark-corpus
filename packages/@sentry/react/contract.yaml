package: "@sentry/react"
semver: ">=0.0.0"
contract_version: "1.0.0"
maintainer: "nark-sh"
last_verified: "2026-06-12"
coverage_status: "out-of-scope"
skip_reason: "low-callsite"
researched_at: "2026-04-03"
coverage_rationale: |
  Sentry SDK is the error reporting layer itself (called from catch blocks). No throwable postconditions for application developers to handle. Callsite pattern is config-only (Sentry.init) and fire-and-forget (Sentry.captureException inside catch).
